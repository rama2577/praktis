
## 00:16 — Video Praktis V3 (VO suara Rama)
- Rama rekam VO sendiri (6 m4a, Mac QuickTime/Voice Memos), minta: tingkatkan kualitas + perbaiki pitch (emosi lebih terasa)
- Proses: highpass 75Hz + afftdn (denoise) + treble +3.5k (presence) + pitch +1 seminada (asetrate/atempo) + trim silence + loudnorm -16 LUFS
- Hasil: vo-rama/seg1..6.wav, total speech ±58s (pace Rama lebih cepat dari neural)
- Output: video-praktis-v3.mp4 (63.8s / 1:04, 1920x1080, 6.5MB) — build3.js
- Catatan: durasi 1:04 (lebih pendek dari target 1:20 karena pace natural); pitch +1 semitone bisa disesuaikan

## 00:25 — Website Praktis + video demo
- Landing page Praktis (project website-f3df21083647c108f9375d2f, fc-nginx, deliveryMode=update) di-update: tambah section #demo dgn <video> (assets/video/praktis-demo.mp4 = video-praktis-v3.mp4, 63.8 detik) + poster assets/poster-demo.jpg (16:9 dari mod-41 dashboard)
- Nav link "Demo" ditambah; CSS .demo/.video-wrap/.video-caption
- Verifikasi: DOM video ok (readyState 4, metadata terbaca), ffprobe 63.8s, projects.json updatedAt di-update → auto-preview ter-trigger
- Video source: projects/praktis-video/ (video-praktis-v3.mp4 final; build3/ = segment intro/dash/pipe/rev/cust/outro; Seg 1..6 .m4a = voiceover)

## 00:40 — Website Praktis re-theme + enhancement + hapus billing
- index.html di-rewrite total: tema Lark light (biru #2b5aed + ungu AI #7c4dff, bg #eef3fa, font Inter) — ganti dark navy+gold
- Enhancement terbaru masuk: hero screenshot dashboard + video demo + 6 fitur visual (editor koreksi, buku besar sekali klik, laporan custom AI, core tax, dropdown COA, aset penyusutan) + section "Untuk Siapa" (5 segmen)
- Billing (#harga) DIHAPUS → CTA diganti mailto:halo@praktis.id ("pilot terbatas, menuju GA")
- Asset baru di website/assets/: 6 screenshot produk (jpg) + poster + video
- Verifikasi: 0 broken image, video readyState 4, AutoGLM PASS (tema Lark konsisten, kontras AAA, 0 overflow)

## Sprint 1 mulai (16 Agu 00:55)
- Rama: "Mulai Sprint 1" + jawab akses Praktis (sudah bisa diakses langsung: HTTPS & SSH reach rama2577/praktis, HEAD 841ce26; clone ke /Users/staff/Documents/praktis-ledgerline — repo besar ~80MB+, masih berjalan di background).
- Sprint 1 increment #1 (commit 863b4c2, V2-M0 fondasi):
  - docs/DESIGN_SYSTEM.md (sumber kebenaran tokens: brand oranye dipertahankan, AI ungu #7c3aed→#c026d3, ink navy, mono tabular untuk angka).
  - tailwind.config.ts: +colors.ai (violet/magenta/soft), +colors.ink, +fontFamily.mono.
  - globals.css: +token --ai-*/--ink-*/.dark.
  - migration_phase44_sprint1_foundation.sql: company_settings.business_profile (retail|fnb), tenants.tier (mikro|middle|enterprise), tabel locations (multi-outlet) + seed default HQ + RLS.
  - src/lib/tier.ts (batas: mikro 50tx/100SKU, middle 200tx/500SKU, enterprise unlimited) + test/tier.test.ts (6 test).
  - signup route: terima profile → set business_profile + mode_fnb.
  - Verifikasi: tsc clean, vitest 110/110, build green.
- **Perlu dijalankan Rama**: migration_phase44 di SQL Editor Supabase produksi (belum di-run).
- Sisa Sprint 1: tier enforcement penuh (meter tx harian + blokir lembut di create_sale), wiring multi-outlet ke stok/penjualan, worker BullMQ+Redis (layanan kedua), onboarding UI profil.
- Catatan: v1 sudah punya plans.ts (gratis/basic/pro/bisnis + max_sku/max_users) — tier v2 (mikro/middle/enterprise) adalah model BARU terpisah; perlu keputusan PM apakah mengganti atau paralel dulu.

## Analisis biaya & harga tier (01:03)
- Keputusan: **tier v2 menggantikan plans v1** (gratis/basic/pro/bisnis → mikro/middle/enterprise).
- Dibuat docs/ANALISIS_BIAYA_TIER.md (commit): target GP ≥75% + gateway 2.5%.
  - COGS/bulan: Mikro Rp27.330 (AI 5.330 + infra 14rb + support 8rb), Middle Rp66.060, Enterprise Rp265.020.
  - Saran harga: Mikro Rp129rb (GP 76,6%) · Middle Rp299rb (75,5%) · Enterprise Rp1,5jt (80,6%). Tahunan diskon ~17%.
  - Konteks: Mikro < Moka/Olsera, Middle kompetitif, Enterprise jauh < SAP/Oracle (0,04% omzet tenant 50M).
  - Sensitivitas: AI 3x → dampak <10% harga; support 2x → Enterprise naik ke ~1,8jt.
  - Asumsi terbesar yg perlu divalidasi: support & infra (AI sudah terukur).
- Langkah berikut: setujui harga → priceIdr di tier.ts + UI billing; refactor plans.ts→tier.ts; tier enforcement.

## Perbandingan kompetitor (01:12)
- Harga tier DISETUJUI Rama (Mikro 129rb/Middle 299rb/Enterprise 1,5jt).
- Dibuat docs/PERBANDINGAN_KOMPETITOR.md: matriks 16 fitur vs 8 kompetitor (Moka/Majoo/Olsera/iSeller/Jubelio/Jurnal/Accurate). Moats Mio: single-entry-cascade (ops+akuntansi nativ), AI/OCR bookkeeping, marketplace berskala, 2 profil 1 produk. Gap: ekosistem pembayaran (Moka/Majoo), distribusi, kedalaman akuntansi (Jurnal/Accurate), breadth marketplace (Jubelio). Harga Mikro 129rb < Moka 299rb.

## Tier enforcement (01:18-01:26)
- Commit e203f1e: tier enforcement blokir lembut.
  - tier.ts +priceIdr (129rb/299rb/1,5jt) + atTxLimit (>= batas → blokir tx berikutnya, beda dr exceedsTxPerDay yg strict >) + priceLabel.
  - tierGuard.ts baru: evaluateTierUsage (murni/testable) + getTierUsage + assertTierWrite → 429 code TIER_LIMIT_REACHED + field upgrade.
  - Wire: POST /api/sale, /api/shopee/sync (assertTierWrite), ingestOrders (hitung remaining, decrement per sukses, break saat habis).
  - GET /api/tier (meter kuota untuk UI).
  - migration_phase45: trigger BEFORE INSERT sales (defense-in-depth, mikro 50/middle 200, enterprise lewat).
  - test/tierGuard.test.ts (7) → total 117 test/17 file. tsc+build hijau.
- Catatan penting:
  - getTenantTier degrades ke "mikro" jika kolom tenants.tier belum ada (phase44 belum di-run) → SEMUA tenant jadi mikro (50tx/hari) sampai phase44+45 dijalankan Rama di Supabase.
  - File tools (edit/write) ter-sandbox ke workspace; file repo Mio di ~/Documents ditulis via exec (cat/python heredoc).
  - priceLabel pakai toLocaleString("id-ID") — OK di Node 22 full-icu.
  - Sisa Sprint 1: wiring multi-outlet (location_id ke stok/sales), worker BullMQ+Redis, onboarding UI, refactor penuh plans.ts→tier.ts (billing/signup masih pakai plans.ts).

## Multi-outlet Phase A (01:25-01:29)
- Commit 7683e0b: dimensi lokasi (aman, tanpa sentuh inti inventori).
  - migration_phase46: sales.location_id (nullable FK -> locations) + index.
  - lib/locations.ts: chooseLocationId (murni) + getLocations/getDefaultLocationId (is_default -> code HQ -> paling lama)/resolveLocationId (requested valid -> default)/setSaleLocation.
  - Wire: POST /api/sale (opsional location_id), /api/shopee/sync, ingestOrders (default lokasi). Update sales.location_id SETELAH create_sale (2 statement, non-breaking ke cascade).
  - API /api/locations: GET + POST (1 default aktif per tenant).
  - schemas: +locationSchema, saleSchema.location_id (uuid).
  - test 119/119 (18 file). tsc+build hijau.
- Keputusan arsitektur: stok per-lokasi = Phase B (inventory_layers/stock_movements + consume_inventory/create_sale + purchase + opname + transfer antar-outlet) — BUTUH staging DB karena menyentuh fungsi inti FIFO/PSAK; jangan dikerjakan tanpa test SQL live.
- Model inventori Mio (untuk referensi): consume_inventory(phase32) FIFO/AVERAGE dari tabel inventory_layers (global per produk, tanpa lokasi); stock_movements = ledger IN/OUT global; fifo_consume = wrapper. create_sale (phase38) panggil fifo_consume per item.
- Sisa Sprint 1: worker BullMQ+Redis, onboarding UI, refactor plans.ts->tier.ts (billing/signup).

## Deploy Redis (01:31)
- Rama minta deploy Redis dulu sebelum BullMQ.
- Topologi: VPS Contabo 147.93.157.45 (Coolify v4.3.2, network `coolify`, Traefik). App container `s6hb8h...` (Nixpacks, port 3000, image tag = commit e203f1e). `coolify-redis` = Redis INTERNAL Coolify (jangan dipakai app).
- Deploy Redis khusus Mio via SSH+docker:
  - container `mio-redis`, image redis:7-alpine, network `coolify` (internal-only, TANPA publish port publik).
  - --restart unless-stopped, volume `mio-redis-data` (/data), appendonly yes + save 60 1000.
  - password random 48 hex (openssl rand -hex 24).
  - REDIS_URL `redis://:<pass>@mio-redis:6379` disimpan root-only 0600 di `/root/.mio-redis-url` di VPS (bukan di repo/chat).
- Verifikasi: PONG (via -a), passlen=48, restart=unless-stopped, vol=mio-redis-data. Catatan: redis-cli `-u` gagal (quirk empty-username) tapi `-a` PONG — ioredis/BullMQ pakai format redis://:pass@host:port, aman.
- Tidak ada Coolify API token lokal → env REDIS_URL harus ditambah Rama via Coolify UI (Settings→Environment Variables) + Redeploy. Bisa digabung saat wire BullMQ.
- Langkah berikut: wire BullMQ+Redis (lib/queue.ts, worker terpisah) — pakai REDIS_URL dari env.

## BullMQ + Redis worker (11:08-11:15)
- Commit 3803e4e: fondasi BullMQ + worker store-sync.
  - deps: bullmq@5.81.3 + ioredis@5.11.1 (WAJIB v5 — bullmq6/ioredis6 butuh Node >=24; produksi NIXPACKS_NODE_VERSION=22, dikonfirmasi di ci.yml) + tsx@4.23.12 (runtime worker).
  - lib/queue.ts: parseRedisUrl (murni, testable) + getQueueConnection (dari REDIS_URL) + getQueue (lazy, cache globalThis agar tahan HMR) + enqueue (no-op false bila tanpa Redis).
  - src/worker.ts: Worker store-sync concurrency 2, processor = runSync per koneksi (re-fetch conn by id+tenant).
  - cron store-sync: enqueue per koneksi (attempts 3 + backoff exponential 5s), fallback inline bila Redis belum ada.
  - tierGuard.ts: NextResponse -> Response (Web) — penting agar worker tidak tarik next/server di jalur import (worker → sync → ingest → tierGuard).
  - package.json: script "worker" = "tsx src/worker.ts".
  - test/queue.test.ts (5) → total 124 test/19 file. tsc + build hijau. Smoke: worker exit "REDIS_URL belum diset" bersih.
- Worker env butuh: REDIS_URL + NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SECRET_KEY + ENCRYPTION_KEY (getAdapter→decryptSecret). TIDAK butuh CRON_SECRET.
- ORDER deploy (PENTING): deploy worker dulu (atau bareng) SEBELUM set REDIS_URL di web app — karena cron enqueue bila REDIS_URL set; tanpa worker → job menumpuk tak diproses. Tanpa REDIS_URL → fallback inline (aman).
- Langkah berikut: onboarding UI (pilih Ritel/F&B) + refactor plans.ts→tier.ts.

## Onboarding UI (11:17-11:21)
- Commit 1d85c67: pemilih jenis usaha (Ritel vs F&B).
  - lib/profile.ts: resolveBusinessProfile (murni) — eksplisit / turun mode_fnb / default retail.
  - components/BusinessProfilePicker.tsx: 2 kartu (Store/UtensilsCrossed), reusable.
  - signup page: pilih jenis usaha + kirim `profile` ke /api/signup (route sudah dukung sejak 863b4c2).
  - setup step 1: pilih jenis usaha (onChange sinkron business_profile + mode_fnb).
  - settings route: simpan business_profile (resolveBusinessProfile), mode_fnb mengikuti (=fnb).
  - test/profile.test.ts (3) → total 127 test/20 file. tsc+build hijau.
- Catatan: signup masih tampilkan plans v1 (gratis/basic/pro/bisnis) — itu sisa item refactor plans.ts→tier.ts (terakhir Sprint 1). Profil bisnis + tier enforcement sudah independen.
- Sisa Sprint 1: refactor penuh plans.ts→tier.ts (signup/billing/enforcement konsolidasi).
