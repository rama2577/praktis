
## 00:16 — Video Praktis V3 (VO suara Rama)
- Rama rekam VO sendiri (6 m4a, Mac QuickTime/Voice Memos), minta: tingkatkan kualitas + perbaiki pitch (emosi lebih terasa)
- Proses: highpass 75Hz + afftdn (denoise) + treble +3.5k (presence) + pitch +1 seminada (asetrate/atempo) + trim silence + loudnorm -16 LUFS
- Hasil: vo-rama/seg1..6.wav, total speech ±58s (pace Rama lebih cepat dari neural)
- Output: video-praktis-v3.mp4 (63.8s / 1:04, 1920x1080, 6.5MB) — build3.js
- Catatan: durasi 1:04 (lebih pendek dari target 1:20 karena pace natural); pitch +1 semitone bisa disesuaikan

## 00:25 — Website Praktis + video demo
- Landing page Praktis (project website-f3df21083647c108f9375d2f, fc-nginx, deliveryMode=update) di-update: tambah section #demo dgn <video> (assets/video/praktis-demo.mp4 = video-praktis-v3.mp4, 63.8 detik) + poster assets/poster-demo.jpg (16:9 dari mod-41 dashboard)
- Nav link "Demo" ditambah; CSS .demo/.video-wrap/.video-caption
- Verifikasi: DOM video ok (readyState 4, metadata terbaca), ffprobe 63.8s, projects.json updatedAt di-update → auto-preview ter-trigger
- Video source: projects/praktis-video/ (video-praktis-v3.mp4 final; build3/ = segment intro/dash/pipe/rev/cust/outro; Seg 1..6 .m4a = voiceover)

## 00:40 — Website Praktis re-theme + enhancement + hapus billing
- index.html di-rewrite total: tema Lark light (biru #2b5aed + ungu AI #7c4dff, bg #eef3fa, font Inter) — ganti dark navy+gold
- Enhancement terbaru masuk: hero screenshot dashboard + video demo + 6 fitur visual (editor koreksi, buku besar sekali klik, laporan custom AI, core tax, dropdown COA, aset penyusutan) + section "Untuk Siapa" (5 segmen)
- Billing (#harga) DIHAPUS → CTA diganti mailto:halo@praktis.id ("pilot terbatas, menuju GA")
- Asset baru di website/assets/: 6 screenshot produk (jpg) + poster + video
- Verifikasi: 0 broken image, video readyState 4, AutoGLM PASS (tema Lark konsisten, kontras AAA, 0 overflow)

## Sprint 1 mulai (16 Agu 00:55)
- Rama: "Mulai Sprint 1" + jawab akses Praktis (sudah bisa diakses langsung: HTTPS & SSH reach rama2577/praktis, HEAD 841ce26; clone ke /Users/staff/Documents/praktis-ledgerline — repo besar ~80MB+, masih berjalan di background).
- Sprint 1 increment #1 (commit 863b4c2, V2-M0 fondasi):
  - docs/DESIGN_SYSTEM.md (sumber kebenaran tokens: brand oranye dipertahankan, AI ungu #7c3aed→#c026d3, ink navy, mono tabular untuk angka).
  - tailwind.config.ts: +colors.ai (violet/magenta/soft), +colors.ink, +fontFamily.mono.
  - globals.css: +token --ai-*/--ink-*/.dark.
  - migration_phase44_sprint1_foundation.sql: company_settings.business_profile (retail|fnb), tenants.tier (mikro|middle|enterprise), tabel locations (multi-outlet) + seed default HQ + RLS.
  - src/lib/tier.ts (batas: mikro 50tx/100SKU, middle 200tx/500SKU, enterprise unlimited) + test/tier.test.ts (6 test).
  - signup route: terima profile → set business_profile + mode_fnb.
  - Verifikasi: tsc clean, vitest 110/110, build green.
- **Perlu dijalankan Rama**: migration_phase44 di SQL Editor Supabase produksi (belum di-run).
- Sisa Sprint 1: tier enforcement penuh (meter tx harian + blokir lembut di create_sale), wiring multi-outlet ke stok/penjualan, worker BullMQ+Redis (layanan kedua), onboarding UI profil.
- Catatan: v1 sudah punya plans.ts (gratis/basic/pro/bisnis + max_sku/max_users) — tier v2 (mikro/middle/enterprise) adalah model BARU terpisah; perlu keputusan PM apakah mengganti atau paralel dulu.

## Analisis biaya & harga tier (01:03)
- Keputusan: **tier v2 menggantikan plans v1** (gratis/basic/pro/bisnis → mikro/middle/enterprise).
- Dibuat docs/ANALISIS_BIAYA_TIER.md (commit): target GP ≥75% + gateway 2.5%.
  - COGS/bulan: Mikro Rp27.330 (AI 5.330 + infra 14rb + support 8rb), Middle Rp66.060, Enterprise Rp265.020.
  - Saran harga: Mikro Rp129rb (GP 76,6%) · Middle Rp299rb (75,5%) · Enterprise Rp1,5jt (80,6%). Tahunan diskon ~17%.
  - Konteks: Mikro < Moka/Olsera, Middle kompetitif, Enterprise jauh < SAP/Oracle (0,04% omzet tenant 50M).
  - Sensitivitas: AI 3x → dampak <10% harga; support 2x → Enterprise naik ke ~1,8jt.
  - Asumsi terbesar yg perlu divalidasi: support & infra (AI sudah terukur).
- Langkah berikut: setujui harga → priceIdr di tier.ts + UI billing; refactor plans.ts→tier.ts; tier enforcement.

## Perbandingan kompetitor (01:12)
- Harga tier DISETUJUI Rama (Mikro 129rb/Middle 299rb/Enterprise 1,5jt).
- Dibuat docs/PERBANDINGAN_KOMPETITOR.md: matriks 16 fitur vs 8 kompetitor (Moka/Majoo/Olsera/iSeller/Jubelio/Jurnal/Accurate). Moats Mio: single-entry-cascade (ops+akuntansi nativ), AI/OCR bookkeeping, marketplace berskala, 2 profil 1 produk. Gap: ekosistem pembayaran (Moka/Majoo), distribusi, kedalaman akuntansi (Jurnal/Accurate), breadth marketplace (Jubelio). Harga Mikro 129rb < Moka 299rb.

## Tier enforcement (01:18-01:26)
- Commit e203f1e: tier enforcement blokir lembut.
  - tier.ts +priceIdr (129rb/299rb/1,5jt) + atTxLimit (>= batas → blokir tx berikutnya, beda dr exceedsTxPerDay yg strict >) + priceLabel.
  - tierGuard.ts baru: evaluateTierUsage (murni/testable) + getTierUsage + assertTierWrite → 429 code TIER_LIMIT_REACHED + field upgrade.
  - Wire: POST /api/sale, /api/shopee/sync (assertTierWrite), ingestOrders (hitung remaining, decrement per sukses, break saat habis).
  - GET /api/tier (meter kuota untuk UI).
  - migration_phase45: trigger BEFORE INSERT sales (defense-in-depth, mikro 50/middle 200, enterprise lewat).
  - test/tierGuard.test.ts (7) → total 117 test/17 file. tsc+build hijau.
- Catatan penting:
  - getTenantTier degrades ke "mikro" jika kolom tenants.tier belum ada (phase44 belum di-run) → SEMUA tenant jadi mikro (50tx/hari) sampai phase44+45 dijalankan Rama di Supabase.
  - File tools (edit/write) ter-sandbox ke workspace; file repo Mio di ~/Documents ditulis via exec (cat/python heredoc).
  - priceLabel pakai toLocaleString("id-ID") — OK di Node 22 full-icu.
  - Sisa Sprint 1: wiring multi-outlet (location_id ke stok/sales), worker BullMQ+Redis, onboarding UI, refactor penuh plans.ts→tier.ts (billing/signup masih pakai plans.ts).

## Multi-outlet Phase A (01:25-01:29)
- Commit 7683e0b: dimensi lokasi (aman, tanpa sentuh inti inventori).
  - migration_phase46: sales.location_id (nullable FK -> locations) + index.
  - lib/locations.ts: chooseLocationId (murni) + getLocations/getDefaultLocationId (is_default -> code HQ -> paling lama)/resolveLocationId (requested valid -> default)/setSaleLocation.
  - Wire: POST /api/sale (opsional location_id), /api/shopee/sync, ingestOrders (default lokasi). Update sales.location_id SETELAH create_sale (2 statement, non-breaking ke cascade).
  - API /api/locations: GET + POST (1 default aktif per tenant).
  - schemas: +locationSchema, saleSchema.location_id (uuid).
  - test 119/119 (18 file). tsc+build hijau.
- Keputusan arsitektur: stok per-lokasi = Phase B (inventory_layers/stock_movements + consume_inventory/create_sale + purchase + opname + transfer antar-outlet) — BUTUH staging DB karena menyentuh fungsi inti FIFO/PSAK; jangan dikerjakan tanpa test SQL live.
- Model inventori Mio (untuk referensi): consume_inventory(phase32) FIFO/AVERAGE dari tabel inventory_layers (global per produk, tanpa lokasi); stock_movements = ledger IN/OUT global; fifo_consume = wrapper. create_sale (phase38) panggil fifo_consume per item.
- Sisa Sprint 1: worker BullMQ+Redis, onboarding UI, refactor plans.ts->tier.ts (billing/signup).

## Deploy Redis (01:31)
- Rama minta deploy Redis dulu sebelum BullMQ.
- Topologi: VPS Contabo 147.93.157.45 (Coolify v4.3.2, network `coolify`, Traefik). App container `s6hb8h...` (Nixpacks, port 3000, image tag = commit e203f1e). `coolify-redis` = Redis INTERNAL Coolify (jangan dipakai app).
- Deploy Redis khusus Mio via SSH+docker:
  - container `mio-redis`, image redis:7-alpine, network `coolify` (internal-only, TANPA publish port publik).
  - --restart unless-stopped, volume `mio-redis-data` (/data), appendonly yes + save 60 1000.
  - password random 48 hex (openssl rand -hex 24).
  - REDIS_URL `redis://:<pass>@mio-redis:6379` disimpan root-only 0600 di `/root/.mio-redis-url` di VPS (bukan di repo/chat).
- Verifikasi: PONG (via -a), passlen=48, restart=unless-stopped, vol=mio-redis-data. Catatan: redis-cli `-u` gagal (quirk empty-username) tapi `-a` PONG — ioredis/BullMQ pakai format redis://:pass@host:port, aman.
- Tidak ada Coolify API token lokal → env REDIS_URL harus ditambah Rama via Coolify UI (Settings→Environment Variables) + Redeploy. Bisa digabung saat wire BullMQ.
- Langkah berikut: wire BullMQ+Redis (lib/queue.ts, worker terpisah) — pakai REDIS_URL dari env.

## BullMQ + Redis worker (11:08-11:15)
- Commit 3803e4e: fondasi BullMQ + worker store-sync.
  - deps: bullmq@5.81.3 + ioredis@5.11.1 (WAJIB v5 — bullmq6/ioredis6 butuh Node >=24; produksi NIXPACKS_NODE_VERSION=22, dikonfirmasi di ci.yml) + tsx@4.23.12 (runtime worker).
  - lib/queue.ts: parseRedisUrl (murni, testable) + getQueueConnection (dari REDIS_URL) + getQueue (lazy, cache globalThis agar tahan HMR) + enqueue (no-op false bila tanpa Redis).
  - src/worker.ts: Worker store-sync concurrency 2, processor = runSync per koneksi (re-fetch conn by id+tenant).
  - cron store-sync: enqueue per koneksi (attempts 3 + backoff exponential 5s), fallback inline bila Redis belum ada.
  - tierGuard.ts: NextResponse -> Response (Web) — penting agar worker tidak tarik next/server di jalur import (worker → sync → ingest → tierGuard).
  - package.json: script "worker" = "tsx src/worker.ts".
  - test/queue.test.ts (5) → total 124 test/19 file. tsc + build hijau. Smoke: worker exit "REDIS_URL belum diset" bersih.
- Worker env butuh: REDIS_URL + NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SECRET_KEY + ENCRYPTION_KEY (getAdapter→decryptSecret). TIDAK butuh CRON_SECRET.
- ORDER deploy (PENTING): deploy worker dulu (atau bareng) SEBELUM set REDIS_URL di web app — karena cron enqueue bila REDIS_URL set; tanpa worker → job menumpuk tak diproses. Tanpa REDIS_URL → fallback inline (aman).
- Langkah berikut: onboarding UI (pilih Ritel/F&B) + refactor plans.ts→tier.ts.

## Onboarding UI (11:17-11:21)
- Commit 1d85c67: pemilih jenis usaha (Ritel vs F&B).
  - lib/profile.ts: resolveBusinessProfile (murni) — eksplisit / turun mode_fnb / default retail.
  - components/BusinessProfilePicker.tsx: 2 kartu (Store/UtensilsCrossed), reusable.
  - signup page: pilih jenis usaha + kirim `profile` ke /api/signup (route sudah dukung sejak 863b4c2).
  - setup step 1: pilih jenis usaha (onChange sinkron business_profile + mode_fnb).
  - settings route: simpan business_profile (resolveBusinessProfile), mode_fnb mengikuti (=fnb).
  - test/profile.test.ts (3) → total 127 test/20 file. tsc+build hijau.
- Catatan: signup masih tampilkan plans v1 (gratis/basic/pro/bisnis) — itu sisa item refactor plans.ts→tier.ts (terakhir Sprint 1). Profil bisnis + tier enforcement sudah independen.
- Sisa Sprint 1: refactor penuh plans.ts→tier.ts (signup/billing/enforcement konsolidasi).

## Refactor plans→tier (11:21-11:31) — Sprint 1 SELESAI
- Commit c978c16: tier v2 menggantikan plans v1 di SELURUH aplikasi.
  - tier.ts +priceIdrAnnual (1.290.000/2.990.000/0) + blurb + tierAmount + tierAnnualDiscountPct + tierLimits + TIER_SELF_SERVICE (mikro/middle).
  - HAPUS src/lib/plans.ts + test/plans.test.ts.
  - getTenantLimits → {tier, max_sku} (drop max_users; v2 tanpa batas user).
  - enforcement products/import: tierLabel + maxSku; users route: hapus blok batas pengguna.
  - signup: tenant mulai tier=mikro (plan kolom tetap diisi "mikro"); hapus pemilih paket di UI signup.
  - billing page: pemilih tier mikro/middle (Enterprise→hubungi) + tierAmount/discount. Landing: 3 kartu tier (middle=populer). checkoutSchema enum mikro|middle.
  - billing.ts computeOrder pakai tierAmount; subscription.ts set tier+max_sku (no max_users); notifications.ts pakai resolveTier maxSku.
  - test: tierBilling.test.ts baru; billing/subscription test dimigrasi ke angka tier (mikro 129rb, middle 299rb). Total 127 test/20 file, tsc+build hijau.
- CATATAN PENTING (keputusan produk implisit): v2 TANPA batas jumlah user (hanya SKU + tx/hari). Signup mulai gratis di tier Mikro (50tx/100SKU), upgrade via /billing. Enterprise self-service = tidak (hubungi via email), hanya mikro/middle di checkout.
- SPRINT 1 LENGKAP (6/6): design tokens, tier enforcement, multi-outlet Phase A, BullMQ+Redis worker, onboarding UI, refactor plans→tier.
- Sisa untuk Rama: run migration phase44/45/46 di Supabase + deploy worker Coolify + set REDIS_URL (lihat catatan BullMQ).

## Sprint 2 = V2-M1 Skala Marketplace (P0) — mulai 11:31
- Target AC: 1.000 order < 5 menit, 0 dobel, stok tak minus saat burst.
- Temuan DB: sales.invoice_no sudah `unique` (global, phase3); sales hanya di-insert via create_sale; external_ref belum ada unique index.
- Increment 1 (efafc5c): migration_phase47 — (a) partial unique index (tenant_id, external_ref) + dedupe lama via window fn; (b) invoice via trigger sequence (ganti random 4-digit). ingestOrders: error 23505 (dup) → dianggap dilewati.
- Increment 2 (464cfaa): migration_phase48 — consume_inventory kunci lapisan stok (perform ... for update) sebelum konsumsi → stok tak minus. ingestOrders: anti-dobel batch (1× query chunked 200 utk semua external_ref, bukan N+1 per order).
- SISA V2-M1: (a) DLQ + observasi metrik per run (BullMQ getJobCounts/getFailed + endpoint admin); (b) batch create_sales RPC (1 transaksi utk banyak order) — ini yang risky (duplikasi logika cascade); (c) webhook-first Shopee/Tokopedia butuh kredensial OAuth per-tenant (blocked eksternal).
- Migration baru utk Rama: phase47 + phase48 (berurutan setelah phase46, idempoten).

## Sprint 2 lanjut + investigasi deploy (11:39-11:45)
- Rama: produksi kosong/aman → boleh push semua langsung tanpa staging.
- Increment 3 (f76d38c): DLQ/observasi — GET /api/admin/queue (getJobCounts + getFailed 0-10, requireAdmin). test getQueue lazy (null tanpa REDIS_URL) → total 128 test. + supabase/RUN_ALL_V2_M0_M1.sql (gabungan phase44-48, 220 baris, utk sekali paste).
- Investigasi akses deploy:
  - .env.local lokal hanya Supabase URL+keys (TIDAK ada DATABASE_URL/connection string DB).
  - VPS: tidak ada psql/supabase CLI; tidak ada connection string DB di /root.
  - Coolify: app uuid s6hb8h261norekmqqtg9jm3y (mio-erp, nixpacks, rama2577/mio-erp). coolify-db bisa diakses `psql -U coolify -d coolify`. /api/v1/deploy → 401 (butuh token). manual webhook github → HTTP 500.
  - KESIMPULAN: saya TIDAK bisa run migration Supabase (butuh connection string/password DB) dan TIDAK bisa trigger deploy Coolify (butuh API token). Perlu Rama.
- Batch create_sales RPC: DIPUTUSKAN TIDAK perlu — 1000 order × ~30ms sequential = ~30 detik (< 5 menit AC); bottleneck sebenarnya anti-dobel N+1 yang sudah dibatch. Hindari ship plpgsql untested.
- YANG PERLU RAMA (deploy produksi): (1) paste supabase/RUN_ALL_V2_M0_M1.sql di SQL Editor; (2) Coolify Redeploy app web; (3) deploy worker + set REDIS_URL. Opsional: beri saya Supabase connection string + Coolify API token supaya saya bisa kerjakan sendiri.

## Praktis (LedgerLine) — Wave 1 + Wave 2 tech debt (11:21-11:47)
- **Wave 1 (6/6 selesai)**: QW-1 demo password via `DEMO_PASSWORD` env + `prisma/reset-demo-password.ts` (rotasi prod DITAHAN sampai POC selesai); QW-2 gitleaks CI; QW-3 5 screenshot-*.ts → `scripts/deck/`; QW-4 `scripts/chrome-path.ts`; QW-5 0 `any` di src (+fix bug slaBreaches rate vs avgMinutes di management-letter); QW-6 coverage badge + threshold branches→35.
- **Wave 2**: F-5 billing kuota-only + paywall SPT (`src/server/billing.ts`, enum BillingMode, model UsageMeter, Firm.billingMode/annualPaidAt, Client.quotaMonthly, `/api/billing/overview`, `/api/clients/[id]/billing`, gate SPT 402, halaman `/dashboard/billing` + nav sidebar). Paywall aktif hanya saat `BILLING_ENFORCE=true` (default off → demo SPT tetap terbuka). F-2 `infra/backup-restore.md`, F-4 `infra/railway-services.md`, F-6 `scripts/smoke-llm.ts`.
- Verifikasi: tsc 0 · vitest **429/429** (57 files, +9 billing) · build OK · deploy Railway web online.
- Blocker Wave 2 tersisa: F-1 object storage (butuh keputusan R2/S3/MinIO) + F-3 secret manager (butuh keputusan provider). Keputusan hold: rotasi password demo ditunda sampai periode POC selesai.
- Migration baru: `20260816044258_f10_billing`.

## Load-test 1000 order (11:45-11:49)
- Commit 98fd9a5: scripts/loadtest-1000.ts — skenario uji AC V2-M1.
  - Buat tenant enterprise uji + seed_tenant_defaults + channel LT + 5 produk (sku LT*-timestamp, stock 200 each = 1000 unit) + inventory_layers langsung.
  - 1000 order unik (qty 1) + 50 dobel → burst 10 chunk konkuren via ingestOrders (kode asli).
  - Verifikasi: <5 menit, imported=1000, skipped=50 (dobel), failed=0, sales=1000, layer stok<0 = 0, ref dobel di DB = 0. PASS/FAIL + exit code.
  - Jalankan: NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SECRET_KEY env, npx tsx scripts/loadtest-1000.ts (butuh migration phase44-48 sudah jalan).
  - Smoke: tanpa env → exit rapi "Set NEXT_PUBLIC_SUPABASE_URL...". Impor chain ingest TIDAK pakai alias @/ (relative semua) → tsx bisa jalan dari scripts/.
- tsc + vitest 128 + build hijau.
- Catatan: test stok pas-pasan (1000 unit = 1000 order) supaya race konsumsi terdeteksi → layer negatif.

## Deploy V2-M1 ke produksi SELESAI (12:13-12:20)
- Rama kasih kredensial Coolify: admin rama.pelatihmlp@gmail.com (screenshot tadi = halaman login, "credentials do not match" krn salah email/pass).
- **Temuan**: API login Coolify (POST /api/v1/auth/login) TIDAK ADA (butuh API token, bukan email/pass). Deploy API butuh token `auth:sanctum` + ability `deploy`.
- **Solusi deploy via SSH**: panggil `queue_application_deployment(...)` langsung lewat `php artisan tinker` (global helper di bootstrap/helpers). Berhasil → status "queued" → deployment_uuid.
- **Fakta penting**: commit 98fd9a5 SUDAH ter-deploy otomatis saat push (deploy plirbshik..., finished 12:15:40 WIB) — auto-deploy webhook ternyata JALAN. Container jalan: image `s6hb8h261norekmqqtg9jm3y:98fd9a58...`.
- Deploy redundan yang kutrigger (jikcw6r...) DIBATALKAN: update status `cancelled-by-user` + `docker rm -f jikcw6r...`.
- **Smoke test produksi**: https://mio-erp.store → HTTP 200 (0.9s); route baru `/api/tier`, `/api/admin/queue`, `/api/locations` → 401 (route ada, auth required) = kode baru LIVE.
- TOOLS.md ditambah section **Coolify** (kredensial + mekanisme deploy via tinker + monitor + cancel).
- Mekanisme deploy sekarang TANPA klik UI dan TANPA API token → deploy berikutnya bisa full otomatis dariku.
- Sisa: worker BullMQ di Coolify belum deploy (REDIS_URL di web belum diset) — masih inline fallback, aman.
- Next: jalankan load-test 1000 order (scripts/loadtest-1000.ts, butuh NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SECRET_KEY dari .env.local).

## Load-test 1000 order DIJALANKAN → PASS (12:20-12:24)
- Hasil: **18.79 detik** (< 5 menit ✓) · throughput 55.9 order/dtk · imported 1000 · skipped 50 (dobel) · failed 0 · sales 1000 · layer stok<0 = 0 · ref dobel di DB = 0 → **PASS semua AC V2-M1**.
- Tenant uji: d5f73823-... (enterprise). Data test DIBERSIHKAN: 43/43 tabel via REST delete (service role) urut child→parent + tenants dihapus. Verifikasi: tenants?id=eq... → [] (bersih).
- Pelajaran: skema FK didapat dari OpenAPI PostgREST (`GET /rest/v1/` + Accept: openapi+json, FK ada di `<fk table=.. column=..>` di description property). Semua tabel bisnis punya `tenant_id → tenants`. Cleanup order: sale_return_items→sale_returns→sale_items→journal_lines→journal_entries→stock_movements→inventory_layers→unit_conversions→sales_order_items→purchase_order_items→products→sales→...→tenants.
- **V2-M1 TERVERIFIKASI di produksi** (throughput, idempotensi, stok lock). Sprint 2 selesai.

## V2-M2 Inc 1 — Core AI engine (12:35-12:42) — commit 6dda03f
- Adaptasi modul AI/OCR Praktis (`~/Documents/praktis-ledgerline/projects/ledgerline/src/ai/`) ke Mio.
- Dibuat `src/lib/ai/`: accountCodes, ruleEngine, validation, docTypes, ocrCost, translate, qr, llm, drafting + knowledge/ (5 file KB).
- **Pemetaan COA Praktis→Mio**: 1-1000→1000 Kas, 1-1200→1100 Piutang, 1-1300→1200 Persediaan, 1-1400→1300 PPN Masukan, 2-1100→2000 Utang, 2-2000→2200 Utang Pajak, 4-1000→4000 Pendapatan.
- Strategi dipertahankan: rule engine dulu (pure), LLM hanya jika confidence<0.6, model routing flash→strong, validasi balance wajib, PPN 11%.
- 34 test baru (test/ai/), total 162 test pass, tsc+build hijau.
- docs/V2_M2_DESIGN.md ditulis (AC + arsitektur + pemetaan + increment).
- Sisa V2-M2: Inc 2 (OCR pipeline: tesseract.js/pdf-parse/mupdf/pngjs/jsqr), Inc 3 (DB phase49 documents + journal_review_queue + API /api/ai/draft + /api/ai/review), Inc 4 (rekonsiliasi bank).
- Mio COA (numerik): 1000 Kas, 1010 Bank/QRIS, 1020 Piutang Marketplace, 1100 Piutang Usaha, 1200 Persediaan, 1300 PPN Masukan, 1500 Aset Tetap, 2000 Utang Usaha, 2100 Utang Bank, 2200 Utang Pajak, 3000 Modal, 4000 Pendapatan, 5000 HPP, 6000 Beban Op, 7000 Pendapatan Lain, 8000 Beban Lain. Group: 1 ASET, 2 KEWAJIBAN, 3 EKUITAS, 4 PENDAPATAN, 5 BEBAN.

## V2-M2 Inc 2+3 — OCR pipeline + DB + API (13:20-13:27) — commit d7e2d4e
- src/lib/ai/parsers.ts: parseFileToText (pdf via pdf-parse, xlsx via SheetJS, gambar via vision LLM glm-4v-flash + strong retry). PDF scan belum didukung (mupdf ditunda).
- src/lib/ai/journalPost.ts: posting draft → journal_entries + journal_lines. Validasi balance + cek COA tenant. FK journal_lines = (tenant_id, account_code) → accounts(tenant_id, code) (phase19).
- migration phase49: ai_documents + ai_journal_reviews (RLS tenant, cascade). PERLU dijalankan di Supabase.
- API: POST /api/ai/draft (teks/file base64→OCR→draft→queue), GET /api/ai/review?status=, POST /api/ai/review/[id] (approve→posting jurnal / reject).
- dep: pdf-parse@^2.4.5. Total test 167, tsc+build hijau.
- Sisa V2-M2: Inc 4 rekonsiliasi bank (match baris rekening koran → sales/receipt). Belum: tessdata/tesseract, mupdf (PDF scan), Supabase storage utk file asli (baru simpan extracted_text).
- Auth: resolveTenant (API key ATAU user login app_users.tenant_id). getServerClient = service role (bypass RLS).

## Supabase DB koneksi + GLM key (14:01-14:08)
- Rama kasih: publishable key sb_publishable_..., secret key sb_secret_..., API URL https://dgaqjgwtjopgbadrszoj.supabase.co, GLM_API_KEY (z.ai) tersimpan di .env.local.
- **Koneksi DB Supabase**: direct `db.dgaqjgwtjopgbadrszoj.supabase.co:5432` = IPv6-ONLY (AAAA 2406:da1c:...), user `postgres.dgaqjgwtjopgbadrszoj`, sslmode=require. Mac lokal TIDAK punya IPv6 → pakai VPS (docker run --network host postgres:15-alpine psql).
- Pooler `aws-0-<region>.pooler.supabase.com:5432` → "tenant/user not found" utk semua region dicoba (pooling kemungkinan disabled / region beda).
- **Password DB `Pr@na300607!` DITOLAK Postgres** (3x konfirmasi "password authentication failed") — padahal Rama bilang itu pass database. Kemungkinan password DB sudah di-rotate/berubah. Rama TIDAK mau reset password.
- Jalan ke depan: minta token Management `sbp_...` (Access Tokens) utk run SQL via Management API, ATAU Rama paste SQL manual (phase49 sudah dikasih).
- GLM_API_KEY sudah masuk .env.local (lokal). Belum di-set di Coolify (perlu env var production).
