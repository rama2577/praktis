# 2026-08-09

## Takeover 2 proyek dari Claude → dikelola oleh AutoClaw (Rama)

Rama mengumumkan takeover 2 folder di `/Users/staff/Documents/Claude/Projects`:

1. **Mio FnB** — folder KOSONG (placeholder saja, dibuat Jul 10). Modul F&B sebenarnya SUDAH dibangun di dalam repo Mio ERP (fase F1–F4: resep/BOM, modifier, tipe order, table management, KDS — lihat git log `feat(fnb-*)`). `RENCANA_FNB.md` = spesifikasi awal yang sudah dieksekusi. Ada `supabase/SEED_FNB_DEMO.sql` (belum di-commit).
2. **POS, Accounting App, Inventory App, simply for UMKM** — repo `mio-erp` lengkap. Remote: `github.com/rama2577/mio-erp` (branch `main`). Stack: Next.js 14 + Supabase (Postgres stored functions, single-entry-cascade), multi-tenant via GUC `app.tenant`.

## Status repo (18:15 WIB)
- Working tree: 1 file modified = `src/app/users/page.tsx` (preset peran "Dapur (KDS)" / "Pelayan (Meja+Kasir)" — WIP belum di-commit).
- Untracked: HANDOFF_PROYEK_MIO_ERP.md, MIGRASI_CONTABO.md, MIGRASI_CONTABO_WRAPUP.md, RENCANA_FNB.md, Perbandingan_MBS_vs_ERP_Indonesia.md, Referensi_Desain_Dashboard_ERP.md, Analisis Kompetitif Mio ERP.docx, `supabase/RESET_DAN_IMPORT_NINO.sql`, `supabase/SEED_FNB_DEMO.sql`.
- Commit terakhir: `18ac016` blog panduan lengkap + 28 gambar (Jul 13).

## Deploy (Contabo, ~95% selesai)
- VPS Contabo 147.93.157.45 (Singapore, Cloud VPS 4), Coolify v4.1.2, app Running port 3000, DNS sudah arah ke IP baru, Traefik Healthy.
- **SISA: perbaiki kolom Domains di Coolify** (masih `...sslip.io`) → `https://mio-erp.store` → Save → Redeploy → verifikasi HTTPS + smoke test login. (Butuh akses Coolify/SSH dari Rama.)
- DB tetap Supabase (`dgaqjgwtjopgbadrszoj`).

## Backlog lain
- iPaymu: KYC belum lolos (checkout sandbox error).
- Cron Coolify (billing + store-sync, pakai CRON_SECRET) belum dipasang ulang.
- HPP 12 SKU Nino masih 0; RESET_DAN_IMPORT_NINO.sql belum tentu dijalankan.
- GSC: daftar + submit sitemap setelah domain live.

## Aturan penting (dari handoff)
- JANGAN `git add -A` — pakai path spesifik (`git add src supabase docs public ...`).
- Jangan track `tsconfig.tsbuildinfo` & folder `Mile-Business-Suite/` (sudah di-ignore).
- Zod schema WAJIB di `src/lib/schemas.ts`, bukan di route.ts.
- Verifikasi: `npx tsc --noEmit`, `npx vitest run`, `npx next build`.

## Evolusi disetujui (18:19 WIB)
- **evo-2026-08-09-mio-erp-handoff** di-approve oleh Rama → MEMORY.md di-update (tambah baris: ambil alih proyek Mio ERP dari Claude). Draft pindah ke `evolution-drafts/approved/`.

## Verifikasi deploy Contabo (18:25 WIB) — MIGRASI SELESAI ✅
- Rama kasih screenshot password root VPS → SSH key id_ed25519 dipasang ke `root@147.93.157.45` (akses tanpa password untuk kelola lanjut).
- Cek DB Coolify: `applications.fqdn` sudah `https://mio-erp.store`; label Traefik container sudah `Host(mio-erp.store)` + letsencrypt (ternyata field sudah diperbaiki, tinggal belum diverifikasi).
- **Hasil tes live:** https://mio-erp.store → HTTP 200 (dari server & lokal); http:// → 302 ke https; cert Let's Encrypt valid s.d. 21 Okt 2026; title "Mio ERP — Aplikasi Kasir, Stok & Akuntansi UMKM"; robots.txt OK.
- **Sisa smoke test:** login admin + akun Nino belum bisa (password ada di catatan pribadi Rama).
- File sementara berisi password (`.openclaw/tmp/ssh-run.exp`) sudah dihapus. Akses SSH: `ssh root@147.93.157.45` (key-based).

## Simpan proyek (18:30 WIB) — commit + push ✅
- Rama minta simpan proyek dulu. Commit `8831102` di-push ke `origin/main` (18ac016..8831102):
  - WIP `src/app/users/page.tsx` (preset peran Dapur/Pelayan)
  - Dokumen: HANDOFF_PROYEK_MIO_ERP, MIGRASI_CONTABO(+WRAPUP), RENCANA_FNB, Perbandingan_MBS_vs_ERP, Referensi_Desain_Dashboard, Analisis Kompetitif.docx
  - SQL: `supabase/RESET_DAN_IMPORT_NINO.sql`, `supabase/SEED_FNB_DEMO.sql`
- MIGRASI_CONTABO.md dicek dulu: nilai secret di dalamnya sudah ter-redaksi → aman di repo.
- Working tree bersih. Proyek siap dilanjutkan kapan saja (kode + dokumen + deploy sudah live).

## Strategi enhancement (18:50 WIB)
- Rama minta masukan tim (10 peran, kelas SAP/Oracle) untuk enhanced Mio ERP → dibuat `STRATEGI_ENHANCEMENT.md` di root repo proyek (belum di-commit, menunggu keputusan Rama).
- Inti: enterprise-grade thinking, UMKM-grade delivery; prioritas #1 = Retur+credit note, e-faktur CSV, reorder engine, UoM, observability, E2E; roadmap 90 hari E1→E2→E3.
- **Update v2 (19:45):** Rama minta tambah Security Engineer + SonarQube/Code Quality Engineer + masukan pemilik proyek, semua digabung jadi satu dokumen dengan roadmap 4 jalur paralel (Produk/Keamanan/Kualitas/Bisnis-Infra) per fase E1–E4. File `STRATEGI_ENHANCEMENT.md` v2 = 200 baris, belum di-commit.
- **Commit v2 (19:48):** `16f72d5` di-push ke origin/main (8831102..16f72d5) — `STRATEGI_ENHANCEMENT.md` v2 (200 baris).
- 18:14 — Rama: "upgrade deck, gunakan v3 sebagai base" → **V4 SELESAI** (`V4 Praktis Deck.html`, 13 slide, hybrid): V3 + portal klien (slide 10: 12-beranda+13-upload; slide 11: 15-analisa+16-wawasan) + angka v4 (klien/junior 25–30, overhead −77%, 1.850→298 −84%, revenue 5–7×, pill-sky ditambahkan, kepercayaan g4 + akses terisolasi, section-num 10/11/12, data-total 13). QC autoglm 04/07/10/11/13: bersih. V1/V2/V3 → archive/. Screenshot shots-v4/.
- 18:24 — Rama: "convert ke PDF" → **V4 Praktis Deck.pdf** (13 halaman, 1920×1080 landscape, 5,3 MB). Bug fix: @media print .deck height auto + overflow visible (sebelumnya hanya 1 halaman karena overflow:hidden body 1080px). Script scripts/export-pdf.js. Verifikasi: pdf-parse 13 halaman semua berisi + thumbnail qlmanage QC (halaman 1 utuh). Catatan: pdf tool gagal ("Unknown model: zai/glm-4.6v") → fallback qlmanage + autoglm.
- 18:30 — Rama: "rendering PDF nya masih pecah" → PDF Chrome print (page.pdf) bermasalah. **Fix definitif: PDF dibangun dari 13 PNG screenshot (shots-v4) via Pillow** (PIL 11.3 tersedia di python3; sips tidak bisa gabung multi-file; pdfkit/sharp ada di node_modules tapi tak perlu). Hasil: `V4 Praktis Deck.pdf` 13 halaman 1920×1080, 1,4 MB, tiap halaman = screenshot yang sudah terverifikasi bersih → dijamin tidak pecah. QC qlmanage thumbnail halaman 1: tajam, utuh. Catatan: PIL save resolution=96.
- 19:18-19:20 — Rama tanya tech debt → audit repo → **docs/TECHDEBT.md** dibuat (17 item: TD-01..17 + 3 scope note). Prioritas tinggi: debug-login route tanpa gate, kredensial hardcoded di scripts E2E, Redis non-prod, storage filesystem lokal, key management manual. Sedang: LLM path belum teruji, KB statis, OCR belum ada, isolasi tenant belum diuji. Ringan: CI belum jalan di GitHub, tooling deck nyasar, coverage UI 0, dll.
- 19:28 — Rama minta saran "dewan 10 peran ERP" (anti-ERP!) → dijawab per peran.
- 19:44 — Rama: gabung SEMUA (10 peran + masukan dia + Security Engineer + SonarQube Engineer) ke dokumen tech debt + susun tahapan. → **docs/TECHDEBT.md di-restrukturisasi jadi dokumen master**: Bagian 1 TD-01..17 · Bagian 2 EN-01..11 (EN-01 Knowledge Platform versioned+approval — masukan Rama; EN-02 Pengenalan Profil Klien COA/laporan baku — masukan Rama; EN-03 feedback loop; EN-04 multi-tenant middleware; EN-05 event/outbox; EN-06 review UX; EN-07 design system; EN-08 portal klien; EN-09 konektor; EN-10 monitoring proses; EN-11 pricing) · Bagian 3 SE-01..06 (OWASP, upload hardening, auth/session, audit akses, retensi, CI security) · Bagian 4 SQ-01..07 (quality gate, SAST, TS strict, complexity, dependency, lint enforce, test pyramid) · Bagian 5 ROADMAP F0..F4 (F0 security+CI quick wins → F1 foundation → F2 knowledge platform → F3 portal klien → F4 scale/monetisasi) + guardrail anti-ERP.
- 19:50-20:00 — Rama: "Eksekusi F0" → **F0 SELESAI** (commit 20fec40 + bb58060):
  - TD-01 ✅ folder debug-login dihapus (route tak pernah ter-commit, 0 referensi)
  - TD-02 ✅ scripts e2e pakai env TEST_PASSWORD (fallback password123); ci.yml + TEST_PASSWORD
  - TD-09/SE-01 ✅ **scripts/e2e-isolation.ts LULUS 8/8** (clients, queues, dashboard, upload lintas-firma 400, exceptions, PATCH 404; kontrol firma B lihat data sendiri)
  - TD-17 ✅ HSTS (prod only) + X-Permitted-Cross-Domain-Policies; CSP → F1
  - SE-06 ✅ script security:audit + step CI (continue-on-error)
  - TD-10 ⏳ workflow diperbarui & diverifikasi lokal (seed→review→upload→isolation semua hijau); butuh PUSH GitHub (repo belum ada remote)
  - Verifikasi: lint 0 error, 89/89 test, build hijau, e2e-review ✓ e2e-upload ✓ e2e-isolation 8/8 ✓
  - ⚠️ Pelajaran: git repo ROOT = workspace (bukan projects/ledgerline) → git add -A menyeret file internal; sudah dibersihkan via .gitignore root + git rm --cached (.openclaw-attachments, .autoclaw, .DS_Store). Jangan commit file runtime internal.
- 20:26-20:40 — Rama: "lanjut F1" → **F1 SELESAI (commit f1d39d4)**:
  - EN-04 fase 1: src/lib/tenant.ts (AsyncLocalStorage + applyTenantFilter pure) + Prisma extension di db.ts (auto-scope query dalam withTenant; User/Firm tidak; ReviewTask via relasi). Unit test 8 kasus. E2E isolasi tetap 8/8 dengan extension aktif (server di-restart build baru).
  - EN-05 fase 1: src/lib/events.ts typed event bus + emit journalApproved/journalException/slaBreach di journal-machine. Unit test.
  - TD-13: component tests StatusBadge + EmptyState → 112/112 test (dari 89).
  - SQ-01: coverage thresholds vitest 31/30/20/30; terukur 32,97/35,23/25/32,3.
  - docs/infrastructure.md (TD-03/04/05/15): target arsitektur produksi + 4 keputusan provider menunggu Rama.
  - F2 = wrap route withTenant, outbox persisten, Playwright CI, KB versioned (EN-01), profil klien (EN-02).
  - Pitfall: db.ts $allOperations return query(applyTenantFilter(...)) → TS error, perlu `as typeof args`.
- 20:53-21:10 — Diskusi infra & bisnis: R2 (Cloudflare, 10GB gratis egress 0), alternatif (Backblaze B2 $0.006/GB arsip), arsitektur stateless (R2+Railway+Upstash), model bisnis value-based → user putuskan **Rp 700rb/klien/bln** (unit economics: margin 92%, break-even 5 klien, skenario 30-150 klien = Rp 21-105jt/bln). User tanya cloud Cina → saran: Alibaba (region Jakarta, trial), tapi rekomendasi LLM **GLM (Zhipu) gratis**.
- 21:06 — **LLM Engine GLM terpasang (commit bc79a18)**: glm-4-flash (teks, gratis) + glm-4v-flash (OCR, gratis) default; glm-4.6 retry; GLM_API_KEY prioritas + LLM_API_KEY compat; visionCompletion + chatJsonWithFallback; parsers/drafting disambung; tests/llm.test.ts 11 kasus → **124/124 test**; docs/infrastructure.md didesain ulang (diagram + AI engine detail); .env.example & TECHDEBT (TD-06) update.
- Keputusan bisnis tertunda (belum dicatat file): pricing Rp 700rb/klien/bln, retensi 15 bln + perpanjangan 12 bln (cap 60), export PDF/CSV/XLSX wajib, R2 primary + B2 arsip, Railway Postgres, Cloudflare DNS. User bilang "jangan update apa2" untuk file bisnis — simpan di memory dulu.

## Enhancement Fase E1 Mio ERP — 12 Agu 2026

### Selesai
- **Retur penjualan + credit note (fitur #1):** commit `67efe33` — migration `phase39_sale_returns.sql` (tabel `sale_returns`/`sale_return_items`, SP `create_return` dengan jurnal balik + restock FIFO + audit log, view `v_sale_returns`, RLS, COA 4001), API `POST /api/returns`, schema zod di `src/lib/schemas.ts`, 8 test (schemas.test.ts). Verifikasi: tsc noEmit ✅, vitest 85/85 ✅, next build ✅. Push ke origin/main. Deploy Contabo auto-triggered, site tetap live (HTTP 200).
- **VPS hardening:** Password auth SSH disabled (`systemctl restart ssh`), fail2ban installed & active (3 IP banned: 45.148.10.147, 176.53.159.198, 45.148.10.183).
- **Node v22.23.2** installed via nvm (v26 incompatible with vitest/rolldown).

### Pending E1
- **Prod Supabase:** migration `phase39` BELUM dijalankan di database produksi → perlu SQL Editor di dashboard Supabase.
- **Port 8000 Coolify:** masih terbuka ke internet → perlu dibatasi (butuh IP admin Rama).
- Fitur lanjutan: e-faktur CSV, reorder engine, UoM konversi, rotasi SUPABASE_SECRET_KEY, gitleaks/npm audit CI, SonarCloud, E2E Playwright, PITR backup, Sentry — menunggu slot berikutnya.

**E-faktur CSV (12:11-12:18):** commit `e9ffefb` — `GET /api/exports/e-faktur?year_month=YYYY-MM`, membaca company_settings (NPWP/nama/alamat penjual) + invoices (join partners untuk NPWP pembeli B2B) + sales POS (NPWP "000000000000000" eceran), CSV 16 kolom format DJP e-Faktur. TSC ✅ build ✅ push ke origin/main. ⚠️ AUTO-DEPLOY BELUM JALAN — Coolify webhook butuh otentikasi; minta Rama deploy manual via UI Coolify (`http://147.93.157.45:8000`, klik Deploy di aplikasi Mio).

## Fase E1 lanjutan — 13 Agu 2026 (commit 8fb78bd, f84fcf3)
- **npm audit 4→2 high:** xlsx diganti ke SheetJS resmi 0.20.3 (CDN tarball). Sisa 2 high = next + postcss transitive → cuma bisa ditutup dengan upgrade Next 14→16 (breaking, tugas strategis → SEC-01 di docs/SECURITY_BACKLOG.md).
- **CI v2** (.github/workflows/ci.yml): node 22, + build blocking, + gitleaks blocking, + npm audit non-blocking (2 known).
- **E2E Playwright**: `e2e/smoke.spec.ts` 3 test (landing title, login form di /pos, login demo admindemo/mio123 → dashboard). Lulus 3/3 vs produksi. Jalan di CI saat push ke main (sekaligus uptime check). Pelajaran: login form muncul di halaman terproteksi, bukan "/" (landing SEO).
- **SonarCloud**: sonar-project.properties + step CI gated `env.SONAR_TOKEN != ''` — aktif saat token diisi.
- **docs/SECURITY_BACKLOG.md** dibuat (SEC-01..11, status terbuka/selesai).
- E1 tersisa yang butuh Rama: port 8000 whitelist (IP admin), rotasi SUPABASE_SECRET_KEY, PITR check, Sentry DSN, SONAR_TOKEN.

## Rotasi SUPABASE_SECRET_KEY — SELESAI (13 Agu, SEC-03)
- Rama generate key baru `sb_secret_...` + screenshot panel Supabase. Verifikasi: key baru HTTP 200, key lama masih aktif (belum di-revoke).
- ENV Coolify di-update via `docker exec coolify php artisan tinker` (Crypt::encryptString — nilai env Coolify terenkripsi Laravel; jangan update plaintext via psql!).
- API Coolify ternyata disabled → di-enable via `instance_settings.is_api_enabled=true`. Token Sanctum format: Bearer `{id}|{plaintext}`; tabel `personal_access_tokens` butuh `team_id` (0 = Root Team). Deploy di-trigger via `POST /api/v1/deploy?uuid=s6hb8h261norekmqqtg9jm3y` → sukses, container baru `s6hb8h261norekmqqtg9jm3y-053613410858` dengan ENV baru.
- Verifikasi: situs HTTP 200, API retur 400 (validasi OK), `.env.local` lokal di-update.
- Cleanup: bearer token dihapus (file + DB row); API Coolify tetap enabled (buat token baru via UI bila perlu).
- **Tersisa dari Rama:** revoke key LAMA di dashboard Supabase (Settings → API) — rotasi baru lengkap setelah itu.

## Fase E2 — API platform & webhooks (13 Agu, commit c743205, 003122f)
- **API platform (c743205):** `src/lib/apiGuard.ts` (rateLimit 120/menit in-memory + withIdempotency via header x-idempotency-key, tabel `idempotency_keys`), rate limit masuk ke `readContext` (semua GET v1), POST `/api/v1/products` & `/api/v1/sales` di-wrap (rate limit + idempotency). OpenAPI spec di `/api/v1/openapi.json`. Migration `phase42_api_platform.sql` (idempotency_keys + webhook_endpoints + webhook_deliveries + RLS).
- **Webhook engine (003122f):** `src/lib/webhooks.ts` — dispatchWebhook (non-blocking), signPayload HMAC-SHA256 → `X-Mio-Signature: sha256=<hex>`, `isPrivateUrl` proteksi SSRF (blokir localhost/10.x/172.16-31/192.168/169.254/100.64/100.127). CRUD `/api/v1/webhooks` (daftar/daftar-update/hapus, tolak URL privat). Hook `sale.created` di POST v1/sales. Cron retry `/api/cron/webhooks` (x-cron-secret, max 5 attempts). Test: 100/100 (9 test baru webhook), tsc ✅, build ✅.
- **E2 sisa:** approval flow, lokalisasi id/en + dark mode, payment gateway (butuh akun Rama), marketplace API resmi (butuh kredensial). **Phase 42 SQL harus dijalankan Rama di Supabase agar webhook punya tabel** (API tulis tetap jalan tanpa itu).

## Fase E2 — Approval flow (13 Agu, commit ad24c0f)
- Migration `phase43_approval_flow.sql`: tabel `approval_flows` (entity purchase_order/journal, conditions.min_amount, approver_role) + `approval_requests` (status pending/approved/rejected, unique per entity) + RLS.
- `create_po` di-rewrite: PO >= min_amount (flow aktif) → status 'pending_approval' + approval_request. `approve_request` → PO status 'sent'; `reject_request` → 'rejected'. `receive_po` di-guard (blokir pending_approval/rejected).
- API: GET /api/approvals (list+filter), POST /api/approvals (buat flow), GET /api/approvals/flows, POST /api/approvals/:id {action, note} — approver diambil dari email login.
- Verifikasi: tsc ✅, vitest 100/100 ✅, build ✅. Phase 43 SQL belum dijalankan di Supabase (perlu Rama).
- **E2 sisa:** lokalisasi id/en + dark mode · payment gateway (butuh akun Rama) · marketplace API (butuh kredensial).
