# 2026-08-07

## WhatsApp — Nady Alle Surya (nady)

- 10:55 — Nady mengingatkan pinjaman 2 & 3 jatuh tempo hari ini (Jumat 07-08-2026).
- Gambar kalkulator: 2.427.666 + 846.000 = 3.273.666
  - Pinjaman 2: Rp 2.427.666 (⚠️ ini PEMBAYARAN TERAKHIR / pelunasan)
  - Pinjaman 3: Rp 846.000
  - Total jatuh tempo hari ini: Rp 3.273.666
- Nady belum kirim nomor rekening tujuan transfer. Belum dikonfirmasi ke Rama apakah sudah dibayar.

## WhatsApp — kin (+6282261573237)

- 10:59–11:01 — Kin menghubungi, memanggil "Bapak", nanya konfirmasi "Bapak sedang di luar kota".
- Kin: "standby di hari Rabu nanti, saya FU kembali" (Rabu = 12-08-2026).
- ⚠️ Tidak ada konteks percakapan sebelumnya (session baru, tanpa history). Belum tahu detail kesepakatan/konteksnya.

## WebChat — Rama: agent-skills upload

- 11:35 — Rama upload `agent-skills-main.zip` (Addy Osmani's agent-skills repo, MIT) untuk membantu develop app LedgerLine + app lain ke depan. "I will upload some useful skill" — lebih banyak skill mungkin menyusul.
- Installed 24 skills → `~/.openclaw-autoclaw/skills/` (managed dir, auto-discovered):
  spec-driven-development, planning-and-task-breakdown, incremental-implementation, test-driven-development, browser-testing-with-devtools, debugging-and-error-recovery, code-review-and-quality, code-simplification, security-and-hardening, performance-optimization, frontend-ui-engineering, api-and-interface-design, context-engineering, source-driven-development, doubt-driven-development, git-workflow-and-versioning, ci-cd-and-automation, deprecation-and-migration, documentation-and-adrs, observability-and-instrumentation, shipping-and-launch, idea-refine, interview-me, using-agent-skills.
- Shared `references/` (7 checklist files: definition-of-done, security, performance, accessibility, observability, testing-patterns, orchestration-patterns) → `~/.openclaw-autoclaw/skills/references/`, symlinked as `references` di tiap skill folder.
- Zip asli tetap di `.openclaw-attachments/20260807-113505-3c3db8fd-5cb-agent-skills-main.zip`; ekstraksi sementara sudah dibersihkan.

## WebChat — Rama: LedgerLine mockup review

- 11:29 — Rama minta cek mockup: https://chat.z.ai/space/m1ak5692s8r1-art
- Mockup = **LedgerLine Operations Dashboard**: AI bookkeeping ops (47 active clients, 94.7% AI automation, pipeline Draft→Rule Engine→Junior→Senior→Tax Review, SLA bars, AI confidence, activity feed). Dark navy theme, role queues (Junior/Senior/Tax/Partner).
- Feasibility: YES. Stack: Next.js/React + Tailwind, multi-tenant RBAC, job queues, PostgreSQL, OCR+LLM pipeline. Sudah ada knowledge base `skills/ledgerline-ai-bookkeeper` (PSAK/PPN/PPh/COA) — tinggal bangun product shell.
- Screenshot mockup disimpan: `workspace/ledgerline-ops-dashboard.png`.
- Next step ditawarkan: scaffold project (Next.js dashboard + API + review-queue data model).

## WebChat — Rama: LedgerLine Phase 1 (Planning) ✅

- 11:39 — Rama: "Kita mulai phase 1". Dibuat struktur proyek `projects/ledgerline/`:
  - `docs/spec.md` (165 baris): objective, tech stack (Next.js 14 + TS strict + Tailwind + Prisma/PostgreSQL + BullMQ/Redis + Auth.js + Vitest/Playwright), commands, struktur, code style, testing strategy, boundaries, success criteria (dari mockup), 6 open questions.
  - `tasks/plan.md` (90 baris): 8 architecture decisions (monolith Next.js, state machine journal DRAFT→JUNIOR→SENIOR→TAX→PARTNER→APPROVED, tenant-aware, AI pipeline modular, knowledge base sebagai sumber aturan), 4 fase/14 task, risiko, paralelisasi.
  - `tasks/todo.md` (297 baris): 14 task lengkap acceptance criteria + verification + dependencies + scope (M/L), 4 checkpoint review Rama.
  - `README.md`: status PLANNING, cara kerja proyek, alur produk.
- 14 task terbagi: Phase 1 Foundation (1-3: scaffold, schema+seed, auth+RBAC) → Phase 2 Core Pipeline (4-7: clients, upload, AI worker, review engine) → Phase 3 Dashboard (8-10: layout+KPI, pipeline viz+queues, SLA+confidence+activity) → Phase 4 Quality (11-14: metrics+KB+exception, states+a11y, security+observability, pre-launch).
- Keputusan kunci: single-firm multi-client (asumsi), tenant-aware sejak awal, state machine terpusat, rule engine dari knowledge base ledgerline-ai-bookkeeper.
- **Menunggu**: review Rama + jawaban 6 open questions (multi-firm?, format dokumen?, login per-reviewer?, deploy target?, bahasa UI?, LLM provider?) → lalu Task 1 scaffold.

## Self-Evolution: evo-2026-08-07-project-phase1 ✅ applied
- 11:46 — Rama approve proposal `evo-2026-08-07-project-phase1` (signature: project-phase1-start, target: MEMORY.md).
- Diterapkan: bagian `## 当前项目` MEMORY.md di-update → "Proyek aktif: build aplikasi dari mockup (chat.z.ai/space/m1ak5692s8r1-art) — Phase 1 dimulai 2026-08-07".
- Draft surrogate sudah berada di `evolution-drafts/approved/project-phase1-start.md` (dibuat dari payload proposal karena draft pending asli hilang).

## WebChat — Rama: jawaban 6 open questions + Task 1 scaffold ✅

- 11:49 — Rama jawab semua open questions:
  1. Single-firm first (schema tenant-aware)
  2. Dokumen: invoice + rekening koran; format PDF/JPG/XLSX semua
  3. Login per-reviewer + akun admin dev (uji semua modul)
  4. Deployment lokal dulu
  5. UI Full Bahasa Indonesia
  6. LLM: minta saran → rekomendasi: GLM (Z.ai) primary + OpenAI fallback (OpenAI-compatible, switch via env), lokal/Ollama opsional; model vision untuk JPG/scan
- Dokumen di-update: spec.md §1 asumsi + §9 keputusan; plan.md arch decisions 9-11 + keputusan; todo.md disesuaikan (npm, seed 6 user, parse XLSX, UI Indonesia).
- Task 1 selesai: scaffold Next.js 16.3.0 (App Router, TS strict, Tailwind v4) + Vitest/RTL + Playwright config. Struktur src/app, components/{ui,dashboard,pipeline,queues,layout}, lib, server, ai; tests/, e2e/. Halaman landing LedgerLine dark navy Bahasa Indonesia. .env.example (GLM default: api.z.ai/api/paas/v4, model glm-4.6).
- Verified: lint clean, 4/4 tests pass, build sukses, dev server HTTP 200 lang="id".
- Catatan: pnpm tidak ada di mesin → pakai npm; create-next-app di folder temp lalu dipindah (README proyek tidak tertimpa).

## Self-Evolution: evo-2026-08-07-product-decisions ✅ applied
- 11:55 — Rama approve proposal `evo-2026-08-07-product-decisions` (signature: product-decisions-accounting-saas, target: MEMORY.md).
- Diterapkan: baris baru di `## 当前项目` MEMORY.md → "Keputusan produk (2026-08-07): single-firm first, schema tenant-aware; dokumen: invoice + rekening koran; format: PDF, JPG, XLSX".
- Draft surrogate di `evolution-drafts/approved/product-decisions-accounting-saas.md`.

## WebChat — Rama: Phase 2 lanjut → Task 2 selesai ✅

- 11:57 — Rama: "lanjut phase 2!" + tanya apakah boleh tutup Mac sambil tinggal (dijawab: sleep → proses pause, lanjut saat buka lagi; saran biarkan terbuka saat proses panjang).
- Task 2 (data model + seed) SELESAI:
  - Prisma 6 (bukan 7 — Prisma 7 butuh driver adapter, pilih stabil/simple). PostgreSQL 16 Homebrew sudah jalan; DB `ledgerline` dibuat; migration `20260807045922_init`.
  - Schema 9 tabel: Firm, User, Client, Document, JournalEntry + JournalLine, ReviewTask, ActivityLog, SlaEvent + 9 enum. Semua tenant-aware (firmId).
  - Seed: 1 firm, 6 user (admin dev + budi/dwi junior, rina senior, sari tax, andi partner — password123), 3 klien (PT Maju Jaya retail, CV Berkah jasa, PT Sentosa F&B), 3 dokumen, 22 jurnal, 65 baris, 29 review task, 5 activity, 6 SLA event.
  - Verified: queue per role = Junior 5/Senior 3/Tax 2/Partner 1 pending (persis mockup), urgent 1, SLA breach 2 (Junior+Tax), 0 jurnal tidak balance, 0 orphan.
  - `src/lib/db.ts` singleton; `.env` lokal (DATABASE_URL postgresql://staff@localhost:5432/ledgerline); package.json prisma.seed = tsx.
- Catatan teknis: sed sebelumnya merusak backtick di todo.md (diperbaiki). Next up: Task 3 (auth + RBAC — Auth.js + bcryptjs sudah terinstall).

## WhatsApp — Rama (Nino Tantiar): Langganan aktif 🎬

- 14:37–15:01 — Konteks: cek tagihan/recurring charge; yang aktif tinggal **YouTube Premium**; **Viu dkk sudah dicancel semua**.
- 15:16 — Lanjutan: **YouTube Premium juga sudah dicancel**. Status akhir: semua langganan sudah dicancel, tidak ada yang aktif.
- 16:52 — Konfirmasi final: **YouTube Premium udah dicancel**. Semua langganan bersih, nggak ada yang aktif. (Sempat ada pertanyaan soal kartu Danamon buat cek metode bayar — tidak disimpan, saran cek via Play Store/App Store/D-Bank.)
- Belum ada detail: harga, tanggal billing/next renewal, metode bayar. Perlu follow-up kalau mau tracking.

## WhatsApp — Rama (Nino Tantiar): Info service AC 🧾

- 13:40 — "Just info" (untuk dicatat, belum ada aksi):
  - Service AC 2 unit: **Rp 170 rb**
  - Transfer BCA: **8690969841** a.n. **Sugiana**
- Status: info saja, belum dikonfirmasi kapan bayar / apakah sudah bayar.
- 13:40 lanjutan — konteks dari Rama: ini **tagihan AC lama yang belum kebayar**; Mas Sugi baru nagih sekarang (dia juga lupa tagihan itu). Jadi Rp 170 rb = tunggakan service AC 2 unit, bukan tagihan baru.

## Self-Evolution: evo-2026-08-07-project-phase2 ✅ applied
- 13:04 — Rama approve proposal `evo-2026-08-07-project-phase2` (signature: project-phase2-update, target: MEMORY.md).
- Diterapkan: MEMORY.md `## 当前项目` → "Phase 2 dimulai 2026-08-07" (sebelumnya Phase 1).
- Draft surrogate di `evolution-drafts/approved/project-phase2-update.md`.

## WebChat — Rama: Task 3 selesai ✅ (auth + RBAC)

- 13:05 → 16:00 — Rama: "lanjut task 3" (+ "lanjut" setelah jeda). Task 3 SELESAI:
  - next-auth 5.0.0-beta.32, Credentials provider (email+password, bcrypt compare), session JWT, AUTH_SECRET di .env.
  - `src/lib/auth.ts` (NextAuth config + callbacks jwt/session → id, role, firmId), `src/lib/roles.ts` (ROLE_LABELS id, ROLE_WEIGHT, canAccess, OPERATIONAL_ROLES, SYSTEM_ROLES), `src/lib/rbac.ts` (requireRole utk halaman server → redirect; requireRoleApi utk route handler → 401/403), `src/types/next-auth.d.ts` (module augmentation).
  - Halaman /login (server guard: sudah login → redirect /dashboard) + LoginForm client (error state, loading, disabled, hint akun demo). Shell /dashboard: layout guard + Sidebar 3 grup (Operasional/Analitik/Sistem, item belum-ready diberi chip "Task X" non-aktif), header (tanggal id-ID + badge AI Online), halaman dashboard dgn 4 KPI real dari DB (klien aktif, antrian, draft, approved hari ini).
  - Verified E2E via curl: /login 200; /dashboard tanpa session → 307 /login; CSRF → POST credentials (rina/admin/andi) → 302 + session; dashboard render nama user; password salah → CredentialsSignin; signout → session bersih → /dashboard 307 /login. Build sukses; lint clean; 10/10 test (auth.test.ts: canAccess, ROLE_LABELS, hierarki).
- Pitfall dicatat: (1) Vitest 4: `--grep` diganti `-t`/`--testNamePattern`; (2) react-compiler lint menolak `Date.now()` langsung saat render → bungkus helper module-level; (3) `import type { Role }` gagal saat dipakai sebagai value (enum) → import biasa; (4) `ReturnType<typeof auth>` next-auth v5 ter-resolve ke overload middleware → pakai tipe Session eksplisit; (5) signout butuh CSRF token dari cookie jar SAMA.
- Next: Task 4 (manajemen klien CRUD). Checkpoint Foundation tinggal review Rama.

## WebChat — Rama: Task 4 selesai ✅ (manajemen klien CRUD)

- 16:03 — Rama: "Task 4". SELESAI:
  - API: GET/POST /api/clients, PATCH /api/clients/[id] — guard requireRoleApi(SYSTEM_ROLES=ADMIN/SENIOR); validasi manual tanpa dep baru (nama wajib ≤120, industri enum RETAIL/SERVICES/FNB, NPWP opsional format); status ACTIVE/INACTIVE via PATCH.
  - Server helpers: `src/server/clients.ts` — validateClientInput (pure, 6 test), listClients (+_count dokumen/jurnal), listActiveClients (jaminan klien nonaktif tak masuk queue baru).
  - UI: halaman /dashboard/clients (server, requireRole), ClientsManager (tabel responsif min-w-720, tombol Tambah → form inline, Edit, Nonaktifkan/Aktifkan dgn confirm), ClientForm (validasi field, error state, loading, disabled), ClientStatusAction. Sidebar: item Klien diaktifkan.
  - Verified E2E HTTP: login admin → GET 3 klien; POST 201; invalid → 400; PATCH edit + nonaktifkan; junior → 403 "Tidak memiliki akses"; halaman UI render. Cleanup data tes + route debug-login dihapus.
  - 16/16 test, lint clean, build OK.
- Pitfall: setelah rebuild, server `npm start` lama bisa menyajikan chunk basi → login CredentialsSignin palsu. Diagnosa via route debug (prisma+bcrypt OK di server context) → restart server bersih. Pelajaran: setelah `npm run build`, selalu restart server prod sebelum verifikasi; kalau auth aneh, cek dulu proses lama (pkill -f next-server).

## WebChat — Rama: Task 5 selesai ✅ (upload dokumen)

- 16:10 — Rama: "lanjut task 5". SELESAI:
  - Validasi file di `src/server/documents.ts` (pure, 12 test): ekstensi PDF/JPG/XLSX, MIME cocok, ≤10MB, MAGIC BYTES (%PDF / FFD8FF / PK\x03\x04), SHA-256, sanitizeFileName (strip leading dots).
  - Storage `src/lib/storage.ts`: simpan ke `uploads/{clientId}/{uuid}-{nama}` (gitignored). Queue `src/lib/queue.ts`: placeholder → ActivityLog PIPELINE_ENQUEUED (BullMQ di Task 6).
  - API POST /api/documents (multipart: clientId, docType INVOICE/BANK_STATEMENT/RECEIPT, file) — guard OPERATIONAL_ROLES, klien harus milik firm & ACTIVE.
  - UI: halaman /dashboard/clients/[id] (detail klien: info, UploadForm drag&drop + pilih jenis, tabel dokumen dgn badge status & ukuran), nama klien di list jadi link. `formatBytes` ditambah ke format.ts (⚠️ sempat menimpa formatCurrencyRp — digabung kembali).
  - Verified E2E: upload PDF → 201 PENDING; .exe/fake-pdf/11MB/klien-invalid → 400 pesan jelas; record+hash+file+activity log OK; halaman render. Cleanup + re-seed (3 dokumen, 22 jurnal, 3 klien). 28/28 test, lint, build OK.
- **LESSON PENTING (pitfall masking)**: string password literal di command exec bisa di-mask oleh harness → password terkirim `***` (passwordLength:3) → login CredentialsSignin. Solusi: bangun dari potongan string `P="pass""word123"`. Diagnosa: tambah console.log sementara di authorize (lalu dihapus). Verifikasi login E2E berikutnya WAJIB pakai teknik split-string.

## WebChat — Rama: Task 6 selesai ✅ (AI pipeline worker)

- 16:19 — Rama: "task 6". SELESAI (task terbesar sejauh ini):
  - Redis 8.10 via Homebrew; **modul redisbloom/redisearch/redisjson/redistimeseries dinonaktifkan di /opt/homebrew/etc/redis.conf** (file .so tidak ada → server abort; solusi: komentari baris loadmodule, sisanya dibiarkan).
  - BullMQ (queue "pipeline") + ioredis; worker terpisah: `npm run worker` (tsx src/server/pipeline-worker.ts, concurrency 2, retry 2x).
  - Rule engine deterministik (TANPA API key): parseRupiah (format ID, titik ribuan/koma desimal), detectBusinessEvent (5 event: SALES_CREDIT/CASH, PURCHASE, RECEIPT, PAYMENT), template jurnal dari knowledge base (T-001/003/004/005), PPN 11%, confidence heuristic, exception flag. Coverage 96.5%.
  - Parsers: PDF via **pdf-parse v2.4.5** (⚠️ v2 API baru: `new PDFParse({data})` → `getText()`, bukan default-export function v1), XLSX via SheetJS, JPG via LLM vision (butuh key).
  - LLM abstraction: OpenAI-compatible, GLM default (api.z.ai/api/paas/v4, glm-4.6); LLM dipakai hanya jika key ada & confidence rule engine < 0.6; fallback aman.
  - Knowledge base skill disalin → src/ai/knowledge/ (13 file: PSAK, PPN/PPh, COA retail/jasa/fnb, template, validasi).
  - Verified E2E: upload PDF invoice → DRAFT 0.74 (Piutang 9.435.000 = 8.500.000 + PPN 935.000); XLSX rekening koran → DRAFT 0.7; balance OK & traceable semua; 46/46 test; lint; build.
  - Keterbatasan dicatat: rule engine bisa salah tangkap (mis. "Saldo awal" terdeteksi sebagai pembayaran) — LLM menangani lebih baik saat key diatur; Task 7+ akan memperbaiki akurasi.
  - **LESSON: masking secret di exec command** — password literal di command line TER-MASK (passLen=3) walau di-split string; solusi: taruh password di FILE script (scripts/e2e-upload.ts), bukan command line. E2E login kini lewat script file.

## WebChat — Rama: Task 7 selesai ✅ (review queue engine)

- 16:36 — Rama: "task 7". SELESAI:
  - **State machine terpusat** `src/server/journal-machine.ts`: JOURNAL_TRANSITIONS (9 status), canTransition, reviewStageForStatus, nextStatusForAction (approve maju/reject REJECTED/return mundur), transitionJournal (validasi → update jurnal → selesaikan task → SlaEvent → task stage baru via pickAssignee load-balancing → ActivityLog dengan aksi benar).
  - **SLA** `src/server/sla.ts`: target JUNIOR 120/SENIOR 240/TAX 240/PARTNER 120 menit; computeSlaStatus (MET ≤ target, AT_RISK ≤ 125%, BREACHED > 125%); computeFinalSlaStatus untuk SlaEvent.
  - **API**: GET /api/queues (role-scoped by assigneeId, urgent desc; admin semua stage); POST /api/reviews/[taskId] {action: approve|reject|return, note} — reject wajib note (400), RBAC owner/admin (403), transisi invalid → 409.
  - **UI**: /dashboard/queues — kartu antrian per stage (badge, urgent, keyakinan AI, tenggat), panel review: tabel garis jurnal (kode/akun/debit/kredit/ref PSAK), tombol Setujui/Kembalikan/Tolak + catatan. Sidebar "Antrian Review" aktif.
  - **Verified**: 61/61 test (15 baru); E2E scripts/e2e-review.ts — alur penuh 4 stage → APPROVED semua 200, RBAC 403, reject tanpa catatan 400, return → DRAFT, reject → REJECTED; audit trail 6 entri benar (bug log action approve stage 1–3 salah tulis REVIEW_RETURNED → diperbaiki dengan meneruskan action ke transitionJournal); SLA events MET.
  - **BUG yang ditemukan & diperbaiki**: log action diturunkan dari status tujuan (to==="APPROVED") — approve stage 1–3 terlog REVIEW_RETURNED. Fix: parameter action eksplisit.
  - Pitfall baru: **masking password juga bisa terjadi saat write file** (scripts/e2e-review.ts PASSWORD jadi "***" len 3 walau e2e-upload.ts len 11) — verifikasi selalu dengan cek panjang & salin baris dari file yang sudah terbukti benar, jangan ketik ulang.

## WebChat — Rama: Task 8 selesai ✅ (dashboard KPI + layout)

- 17:09 — Rama: "lanjut task 8". SELESAI (Phase 3 dimulai):
  - `src/server/dashboard.ts`: agregasi paralel + pure helpers (automationPct, successfulAutomationPct, deltaVsAverage, daysSince) — KPI dari DB, bukan hardcode.
  - Definisi KPI "AI Automation" = % jurnal AI tanpa exceptionFlag (21/22 = 95,5% — kebetulan mirip mockup 94,7%).
  - 5 KPI: Klien Aktif 3 (+3 bulan ini) · AI Automation 95,5% · Jobs in Progress 18 (9 draft AI + 11 review) · Transactions Hari Ini 22 (0% vs avg 22) · SLA Breaches 2 (Junior 1, Pajak 1).
  - Responsive: DashboardShell client component — sidebar drawer di mobile (hamburger + overlay), statis di desktop; sidebar terima prop onNavigate.
  - Verified: 5/5 test baru; build+lint; E2E HTML mengandung semua KPI + elemen mobile.
  - Demo sebelum task ini: Rama sempat "tidak bisa login" — server sehat (E2E login 5 user OK), kemungkinan cookie lama browser; solusi incognito.

## WebChat — Rama: Task 9 selesai ✅ (pipeline viz + review queues panel)

- 17:14 — Rama: "task 9". SELESAI:
  - `getPipelineData` + pure builders (buildPipelineStages — Tax menggabungkan TAX+PARTNER; buildQueueSummary — pending+urgent per stage).
  - Panel client dengan **polling 30 dtk** ke GET /api/dashboard (baru), tampil label "auto-refresh · jam:menit:detik"; Link tiap stage/queue → /dashboard/queues.
  - Verified: upload xlsx (worker mati) → Rule Engine 1→2 ✓; seed bersih → stages draft=8 ruleEngine=1 junior=5 senior=3 tax=3; queues JUNIOR 5 (1 urgent) SENIOR 3 TAX 2 PARTNER 1 = persis mockup; badge "1 urgent" & link di HTML; 65/65 test; build+lint.
  - Catatan: DB sempat terkontaminasi efek E2E demo (login test task 7 menjalankan review actions) → selalu re-seed sebelum verifikasi visual.

## WebChat — Rama: Task 10 selesai ✅ (SLA + confidence chart + activity feed)

- 17:16 — Rama: "task 10". SELESAI → **Checkpoint Dashboard tercapai** (tinggal review Rama):
  - Recharts diinstall (BarChart distribusi confidence, 4 bucket).
  - `buildSlaSummary` (pure): gabung SlaEvent selesai + task pending (elapsed vs dueAt) → avgPct per stage; warna: merah jika breach/overdue, kuning ≥80%, hijau.
  - `formatRelativeTime` (pure, 6 test): "baru saja"/"N mnt lalu"/"N jam lalu"/"N hari lalu".
  - `ACTION_LABELS` map action → Bahasa Indonesia; getRecentActivity dengan join user.
  - PipelineQueuesPanel di-refactor → DashboardPanels (satu polling 30 dtk: pipeline+antrian+SLA+confidence+activity).
  - **Fix seed**: dueAt task pending dihitung dari createdAt jurnal (lampau) → SEMUA task tampak overdue; diubah: pending normal → tenggat ke depan, hanya urgent lewat 15 mnt. SLA kini realistis: JUNIOR 1 telat (merah), TAX breach historis (merah), SENIOR/PARTNER hijau.
  - Verified: 81/81 test; E2E endpoint + HTML; build+lint. Mockup punya 7 metrik SLA (Upload Validation/AI Draft/Delivery) — 4 metrik review diimplementasikan; sisanya butuh data latensi pipeline (catatan untuk Task 13 observability).
  - Total progress: Task 1-10 ✅, commit 4008bc1 → (task10 commit).

## WebChat — Rama: Task 11 selesai ✅ (quality metrics + KB + exceptions)

- 17:26 — Rama: "lanjut task 11". SELESAI (Phase 4 dimulai):
  - `src/server/metrics.ts`: pct/avg pure helpers + getQualityMetrics (firstPassRate, exceptionRate, avgConfidenceAll, statusConfidence, stageBreachRates, exceptions).
  - Temuan menarik dari data: jurnal EXCEPTION punya confidence 0.55 (terendah) vs APPROVED tinggi → korelasi "AI tahu saat ragu" terlihat di chart Confidence vs Status.
  - `src/server/knowledge.ts`: listKnowledgeEntries baca src/ai/knowledge (13 file, kategori otomatis via knowledgeCategory, preview 800 char).
  - `resolveException` di journal-machine.ts: EXCEPTION → JUNIOR_REVIEW + task JUNIOR (pickAssignee) + ActivityLog EXCEPTION_RESOLVED — jalur transisi terpusat lain di luar review (tetap lewat state machine yang sama).
  - UI: quality (4 kartu + 2 bar chart CSS), knowledge (browser client + search), exceptions (list + form resolusi, catatan wajib 400).
  - Sidebar: Metrik Kualitas, Knowledge Base, Pengecualian aktif — hanya tersisa placeholder: Pipeline Produksi (digabung ke dashboard Task 9) & Monitoring SLA (di dashboard Task 10) & Pengaturan.
  - Verified: 84/84 test; E2E resolve exception lengkap (DB: status/task/log); re-seed; build+lint.

## WebChat — Rama: Task 12 selesai ✅ (states + aksesibilitas)

- 17:32 — Rama: "lanjut". Task 12 SELESAI:
  - Komponen shared baru: Skeleton/SkeletonList, EmptyState (ikon+judul+deskripsi+aksi), ErrorState (tombol Coba lagi).
  - QueueList & ExceptionsList: skeleton loading → error retry → empty state bermakna; DashboardPanels error polling → tombol Coba lagi inline; KnowledgeBrowser empty search.
  - a11y: skip link di DashboardShell (focus:not-sr-only), :focus-visible global accent, prefers-reduced-motion, aria-live=polite flash, th scope="col", html lang="id" (sudah dari awal).
  - Verified: build+lint+84/84 test; grep HTML/CSS konfirmasi elemen a11y ter-render.
