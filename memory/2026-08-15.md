# 2026-08-15

## Pagi — Top-up GLM $4 + fix storage worker
- Rama top-up $4 z.ai → saldo aktif (verifikasi glm-4.5-air sukses, bukan 429 lagi)
- Harga aktual z.ai: Air $0.2/$1.1, glm-4.5 vision $0.6/$2.2, glm-4.6 $0.6/$2.2
- **Bug kritis ditemukan**: Railway volume hanya 1 service → worker ENOENT baca file upload (web & worker beda filesystem)
- **Fix**: worker fetch file dari web via HTTP internal (token) — endpoint /api/internal/files/[...path] + storage.readStoredFile fallback WORKER_MODE; env STORAGE_INTERNAL_TOKEN + WEB_INTERNAL_URL
- **Verifikasi alur AI end-to-end PROD**: upload invoice → PROCESSED, jurnal 1/3 baris DRAFT, ocrMetric pdf-text estCostUsd=0
- Pelajaran: railway volume attach hanya 1 service; path endpoint harus strip prefix "uploads" (double-prefix → 404)

## 11:14 — Pesan final ke Om Cikuy TERKIRIM
- Rama konfirmasi sudah kirim pesan WA ke Om Cikuy (link demo + akun POC poc@ledgerline.dev + akun demo + manual book v2 + link portal 3 klien)
- Saldo GLM $4 aktif, alur AI end-to-end sudah terverifikasi di prod
- Status: menunggu Om Cikuy mencoba → pantau aktivitas tenant POC (login/upload pertama)

## 12:45 — Fase 0 + Fase 1 (T1.1) selesai
- Fase 0: schema EnrichSource + kolom aiDescription/enrichConfidence/enrichedAt/descriptionSource (migrasi prod OK); service `ai-enrich.ts` (enrichJournalLines batch + describeJournal)
- Fase 1 T1.1: `POST /api/journals/describe` + tombol "✨ AI" di form jurnal manual (badge sumber AI/RULE); describeJournal service + fallback deterministik
- Verifikasi: tsc 0 · vitest 388/388 · build OK · E2E prod: endpoint describe 200 {"description":"Penjualan Tunai","source":"AI"} · tombol ✨ AI PASS + screenshot mod-17
- Pelajaran: hati-hati edit tool (sempat hapus body fungsi enrichJournalLines — restore); password demo "password123" (bukan placeholder ***)

## 13:05 — Fase 1 T1.2 selesai
- enrich master klien: parseClientEnrichment (NPWP normalize 15 digit + industry enum + alamat) + enrichClientMaster (persist hanya saat taxId kosong / industry OTHER)
- POST /api/clients/[id]/enrich + tombol "✨ Enrich" di halaman klien (client-enrich-button.tsx, badge hasil + confidence)
- Verifikasi: tsc 0 · vitest 393/393 (+5 test client-enrich) · build OK · E2E prod: endpoint 200 ekstrak NPWP/industri/alamat (confidence 1) · tombol ✨ Enrich PASS + screenshot mod-18

## 13:10 — Fase 1 T1.3 selesai
- inbox cerdas: getDailyBrief (ringkasan harian + antrian review terurut) + buildBriefSummary (deterministik, tanpa LLM) + GET /api/dashboard/brief + panel "Hari Ini" di dashboard
- Verifikasi: tsc 0 · vitest 395/395 (+2 test brief) · build OK · E2E prod PASS panel Hari Ini + screenshot mod-19

## 13:15 — Fase 1 T1.4 selesai (FASE 1 LENGKAP)
- alert anomali: detectAnomalies (SLA BREACHED / BankMutation UNMATCHED / Journal EXCEPTION) + buildAnomalies (pure) + render ⚠️ di panel Hari Ini
- Verifikasi: tsc 0 · vitest 398/398 (+3 test anomaly) · build OK · E2E prod PASS + screenshot mod-20
- FASE 1 (P0) LENGKAP: T1.1 deskripsi AI · T1.2 enrich master · T1.3 inbox cerdas · T1.4 alert anomali

## 13:30 — FASE 2 (P1) LENGKAP
- T2.1: command bar AI ⌘K — answerCommand (intent ask/draft/explain/help + konteks firma) + POST /api/ai/command + AiCommandPalette di header (tombol Tanya AI + ⌘K)
- T2.2: auto-translate — looksLikeForeign (heuristik stopwords EN) + translateToIndonesian + integrasi pipeline (reference & drafting)
- T2.3: parse QR e-Faktur — decodeQrPng (jsqr+pngjs) + parseEfakturQr (NPWP/invoice/DPP/PPN) + integrasi ocrScannedPdf
- Verifikasi: tsc 0 · vitest 409/409 (53 files) · build OK · E2E prod: /api/ai/command 200 (jawab data firma nyata "Klien aktif: 3") · command palette PASS + screenshot mod-21
- Pelajaran: password demo "password123" (jangan placeholder *** di script e2e)
