# 2026-08-15

## Pagi — Top-up GLM $4 + fix storage worker
- Rama top-up $4 z.ai → saldo aktif (verifikasi glm-4.5-air sukses, bukan 429 lagi)
- Harga aktual z.ai: Air $0.2/$1.1, glm-4.5 vision $0.6/$2.2, glm-4.6 $0.6/$2.2
- **Bug kritis ditemukan**: Railway volume hanya 1 service → worker ENOENT baca file upload (web & worker beda filesystem)
- **Fix**: worker fetch file dari web via HTTP internal (token) — endpoint /api/internal/files/[...path] + storage.readStoredFile fallback WORKER_MODE; env STORAGE_INTERNAL_TOKEN + WEB_INTERNAL_URL
- **Verifikasi alur AI end-to-end PROD**: upload invoice → PROCESSED, jurnal 1/3 baris DRAFT, ocrMetric pdf-text estCostUsd=0
- Pelajaran: railway volume attach hanya 1 service; path endpoint harus strip prefix "uploads" (double-prefix → 404)
