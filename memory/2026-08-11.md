# 2026-08-11

## WhatsApp — kin (+6282261573237)

- 10:00 — Kin salam pagi, doakan kesehatan. Kin tanya: "Bapak hari ini belum standby di Jabodetabek ya?" dan sebelumnya "Bapak di luar kota?".
- Pola: tampaknya ada rencana/kesepakatan Rabu (12-08) di area Jabodetabek; Kin akan FU hari Rabu.
- Saya jawab sebagai asisten: tidak bisa memastikan posisi Bapak, tawarkan meneruskan pesan ke Bapak Rama.

## WhatsApp — Rama: Makanan kucing habis 🐱

- 15:22 — Rama: "Yank makanna kucing abis" (harus restock).
- 15:40 — Kirim lokasi: **-6.416638, 106.849388** (Cibubur/Depok area).
- Deskripsi tempat: **"Warna hijau bannernya sebelah tukang telor"** + **"Ada banner CV ponpin 33"** — toko fisik, bukan online.
- Sharelock link produk masih ditunggu. Merek, jenis, harga belum diketahui.

## WhatsApp — Rama: Link Shopee produk Rosellaku 🛒

- 19:51 — Rama kirim sharelink Shopee produk sendiri: **ROSELLAKU – TEH ROSELLA PREMIUM Rp 19.500**
- Link: https://id.shp.ee/6pXcVvzQ
- Catatan: harga di Shopee Rp 19.500, beda dengan harga manual 30gr Rp 15.000 (kemungkinan ukuran berbeda / harga jual online).

## EN-10 metrik per clerk & firma — SELESAI ✅
- `1bf0ef5` implementasi: `src/server/metrics.ts` getClerkMetrics/getFirmMetrics (Map key `${journalEntryId}:${stage}`), API `/api/metrics` (SYSTEM_ROLES), UI "Performa Tim" di /dashboard/quality (4 KPI firm + tabel clerk).
- `ae75c0d` fix seed: reviewedAt relatif createdAt jurnal (offsetMinutes), createdAt task selesai = reviewedAt−25mnt → durasi positif; SlaEvent per task ter-link journalEntryId (sebelumnya null → SLA 0%).
- Verifikasi live: RATA-RATA REVIEW 25 mnt (sebelumnya −353), SLA MET 77,8%, SLA breach Pajak 2/3 · Partner 2/2, konsisten tabel clerk (Sari 33,3% · Andi 0%).
- Pitfall baru (37): metrik durasi = reviewedAt − task.createdAt; seed harus set createdAt task selesai di masa lalu, bukan default now.
- Pitfall (38): re-seed recreate firm → session lama (firmId) jadi stale → semua dashboard 0 → harus logout/login ulang. Portal token juga hilang (cascade client) → recreate via ensurePortalToken (token baru: Maju Jaya 01882943…, Berkah ffaf7e79…, Sentosa c826fd57…).
- Test 164/164 · lint 0 · build hijau. Commit: 1bf0ef5, ae75c0d.

## EN-04 wrap route tenant — SELESAI ✅ (commit 9d0e89b)
- `withTenantApi` (typed ctx) wrap 18 route API: auth 401 tanpa firmId + withTenant() → Prisma extension auto-scope semua query per firma.
- applyTenantFilter v2: filter HANYA operasi baca kolektif/bulk-write (findMany/findFirst/count/updateMany/deleteMany); operasi singular (findUnique/update/delete/upsert) DI-SKIP karena where harus unique → cegah crash Prisma (ownership diverifikasi manual di route).
- Test tenant: 8 → 11 kasus (FILTERABLE_OPS + singular skip). Total 165 test hijau.
- E2E isolasi lintas-firm 8/8 LULUS dengan tenant context aktif.
- Verifikasi live: dashboard penuh data, queues 11 task, approve JUNIOR→SENIOR sukses (state machine + operasi singular aman).
- Dikecualikan dari wrap: auth, health, debug-login, portal (token-based).
- Pitfall baru (39): Prisma findUnique/update/delete TIDAK menerima field non-unique di where → filter otomatis harus skip operasi singular; degrade ke findFirst/updateMany mengubah return type.

## EN-07 Design system formal — SELESAI ✅ (commit ebcb262)
- docs/design-system.md: token formal (warna, tipografi, spacing, motion, grain, konvensi, mapping role).
- Komponen library baru: Card/CardHeader/CardBody, Button (primary gold/secondary/ghost/danger), Table primitives (TH dukung className, TD numeric=font-mono), Badge (alias StatusBadge backward-compat).
- Dashboard per role: DashboardPanels prop `focus` (FOCUS_ORDER: junior=pipeline→activity→sla→insight; senior=sla→pipeline→insight→activity; tax=pipeline→sla→insight→activity; partner=sla→insight→confidence→pipeline→activity; admin=penuh) + ROLE_FOCUS/subtitle per role di page.
- Quality page: tabel clerk → Table primitives; section → Card.
- Verifikasi: 174 test (ui-library +9), build hijau, SSR semua 5 role dicek via curl (urutan h2 DOM — hati-hati RSC payload & sidebar mencemari grep; pakai regex <h2>).
- Pitfall (40): verifikasi urutan section via curl — RSC payload berisi teks section (string serialized) sehingga html.find()/grep biasa menyesatkan; pakai regex <h2>…</h2> yang hanya muncul di DOM asli (sidebar bukan h2).

## EN-06 batch approve (confidence gate) — SELESAI ✅ (commit bf72ea0)
- Server `journal-machine.ts`: `BATCH_APPROVE_CONFIDENCE_MIN = 0.85`, `selectBatchApprovable()` pure (pisah task ≥ threshold vs skipped), `batchApproveTasks()` (PENDING milik firma + filter assignee non-admin, loop sequential `transitionJournal` → SLA/outbox/activity tetap tercatat, return `{approved, skipped, threshold}`).
- Route `POST /api/reviews/batch` (baru): `withTenantApi` + `requireRoleApi(OPERATIONAL_ROLES)`, validasi taskIds 1–50, error → 409.
- UI `queue-list.tsx`: checkbox per task (disabled + tooltip < 85%), sticky batch bar saat seleksi, flash "✅ N disetujui · M dilewati", tip threshold; konstanta lokal sinkron server.
- Fix TS TDZ: submitBatch referensikan load sebelum deklarasi → blok batch dipindah setelah definisi load.
- Test: +5 di tests/review.test.ts (threshold default, seleksi murni, threshold 0, list kosong, fungsi ada) → 179 hijau (22 files), tsc/lint/build OK.
- Verifikasi live (browser): checkbox tampil semua eligible (86–95%), batch bar "1 task dipilih · confidence ≥ 85%" (muncul setelah keyboard Space — klik murni tidak toggle di aksesibilitas tree; aman), klik Setujui 1 → flash "✅ 1 jurnal disetujui sekaligus", Junior 4→3, Senior 4→5 (task pindah stage), DB: task JUNIOR APPROVED + reviewedAt terisi, task SENIOR PENDING baru, SlaEvent JUNIOR MET 77 + ActivityLog REVIEW_APPROVED tercatat.
- Catatan: curl login ke API gagal simpan session cookie (Auth.js Secure) → verifikasi via UI browser + psql langsung.
- EN-06 sisa: **exception one-screen** (dokumen + draft + aturan satu layar saat resolve exception).

## Data demo tambahan (12:25, commit 56833e3)
- User minta data buat demo → buat `scripts/seed-demo-extra.ts` (idempotent, bisa dijalankan ulang setelah re-seed): `npx tsx scripts/seed-demo-extra.ts`.
- Mengisi area yang kosong: ClientProfile 3 (Maju Jaya READY + Berkah READY + Sentosa LEARNING, coaMapping/reportTemplates/rules/sourcePeriod 2026-07), OutboxEvent 3 (journalApproved PROCESSED + NotificationLog SENT, slaBreach FAILED retry 2/3, reportReady PENDING), WebhookSubscription 2.
- **Pitfall format coaMapping**: tipe `Record<kodeKlien, {accountCode, accountName}>` — BUKAN `Record<string,string>`; kalau string, coaMappingHint menampilkan "undefined undefined" (verifikasi via network capture: API 200 tapi hint broken). Perbaiki & update existing (bukan skip) → hint benar: "1000 → 1-1100 Kas dan Setara Kas".
- Verifikasi live: halaman klien → detail PT Maju Jaya → badge "Siap Dipakai" + "6 akun klien sudah dipetakan". Catatan: snapshot browser sering menangkap state loading ("Belum dipetakan" + "Memuat profil…") — wait 3 dtk lalu snapshot lagi.

## Skill keuangan-akuntansi-indonesia — TERSIMPAN ✅ (16:11)
- Instal ke `~/.openclaw-autoclaw/skills/keuangan-akuntansi-indonesia/` (SKILL.md + references/01–06): 01 prinsip & siklus akuntansi, 02 akuntansi biaya, 03 manajemen keuangan, 04 pajak Indonesia (2025–2026, Coretax, PPN 11% efektif), 05 SAK/PSAK (penomoran baru), 06 laporan & dashboard.
- Cara: tulis ke `.openclaw/tmp/skill-keuangan/` (file tools sandbox ke workspace) lalu `cp -R` via exec ke `~/.openclaw-autoclaw/skills/`. Total 62.885 bytes. Pitfall baru (41): file tools TIDAK bisa menulis ke luar workspace → staging + exec cp.
- Konten dari user (masukan KB untuk Praktis); rencana: seed ke KnowledgeItem (EN-01) saat implementasi KB.

## DRAFT PRD v2 — dibuat (16:15, commit 161dc55)
- `projects/ledgerline/docs/PRD-v2-draft.md` — revisi PRD dari masukan teman M1–M9 + masukan akuntan/klien + integrasi skill KB.
- M1 edit saat review + feedback KB · M2 9 kategori dokumen · M3 trial balance · M4 buku besar + reclass + kunci partner · M5 jurnal manual/penyesuaian · M6 bank rekonsiliasi AI · M7 laporan custom AI · M8 aset tetap (komersial+fiskal) · M9 output Core Tax (SPT/e-Faktur, kode pajak per baris).
- Roadmap baru: F2.5 siklus akuntansi inti (M5→M1→M2→M3→M4), F5 aset & kepatuhan pajak (M8→M9), F6 laporan cerdas (M6→M7).
- 8 pertanyaan terbuka [KEPUTUSAN] untuk user: urutan fase, pagar aset, pagar dokumen klien, integrasi Core Tax (export vs PJAP API), cakupan laporan, klien lihat jurnal, snapshot laporan, seed KB bertahap.
- Belum final — menunggu koreksi user sebelum jadi dokumen final & turun ke TECHDEBT/tasks.

## BUILD-PLAN-v2 + Milestone F2.5A Jurnal Manual ✅ (16:26–16:47)
- User: "ok kita mulai pembangunannya" → konfirmasi mulai Milestone 1 (F2.5A · jurnal manual) dari BUILD-PLAN-v2.md (commit 1633118, docs/ledgerline/BUILD-PLAN-v2.md — 11 milestone: F2.5A jurnal manual → F2.5B edit saat review → F2.5C 9 kategori dokumen → F2.5D trial balance → F2.5E buku besar+lock → KB seed → F3 portal → F5A aset → F5B Core Tax export → F6A bank rekonsiliasi → F6B laporan custom AI).
- **F2.5A selesai commit 22abe65**:
  - Schema: enum `JournalType` (AI/MANUAL/ADJUSTING) + `journalType` (default AI) + `entryDate` (default now); migrasi `add_journal_type_and_entry_date`.
  - `src/server/manual-journal.ts`: `journalIsBalanced()` pure (≥2 baris, tidak negatif, tidak debit+kredit bersamaan, total debit=kredit toleransi 0.005), `validateManualJournalInput()`, `createManualJournal()` (langsung APPROVED, createdByAi=false, ActivityLog JOURNAL_CREATED + detail balance), `listManualJournals()` (filter clientId/from/to).
  - API `src/app/api/journals/manual/route.ts`: POST (ADMIN/SENIOR/PARTNER) + GET (OPERATIONAL_ROLES), tenant-scoped.
  - UI `/dashboard/journals` + `components/journals/journal-manager.tsx`: form inline (klien/tanggal/deskripsi/jenis MANUAL|ADJUSTING, baris dinamis, live total + badge Seimbang/Belum seimbang), daftar tabel. Sidebar: menu "Jurnal Manual" di grup Operasional.
  - Fix pre-existing TS error di seed-demo-extra.ts (coaMapping → Prisma.InputJsonValue).
  - Test: +12 (191/191, 23 files), tsc 0 error, lint 0, build OK.
  - Verifikasi live: form terisi via autoglm-browser-agent (select dropdown pakai select_dropdown_option — side panel keyboard bridge tidak bisa ubah native select!), "✓ Seimbang" muncul, submit sukses; DB: jurnal ADJUSTING APPROVED entryDate 2026-08-11 PT Maju Jaya, lines 6-1400 Dr 250.000 / 1-1500 Cr 250.000, ActivityLog JOURNAL_CREATED; UI daftar tampil "Penyesuaian · Disetujui · 2 baris".
  - **Pitfall baru (46)**: native `<select>` TIDAK bisa diubah lewat visible_browser keyboard/type bridge — pakai autoglm-browser-agent `select_dropdown_option`.
- Dev server direstart (Prisma Client lama di memori setelah migrate → route baru 500; kill + `npm run dev` ulang, log ke /tmp/ledgerline-dev.log). Selanjutnya hati-hati: setelah `prisma migrate dev`, dev server yang sedang jalan perlu restart agar enum baru dikenal.
- TECHDEBT.md: baris roadmap F2.5A ✅ + F2.5B next.
- Next: F2.5B edit saat review (PRD M1).

## F2.5B Edit saat review — SELESAI ✅ (commit 9326f01, 17:05)
- Schema: `JournalCorrection` (firmId, journalEntryId, userId, stage, field, accountCode, before/after Json, createdAt) + relasi User "CorrectionUser" & Firm. Migrasi `add_journal_correction`.
- `src/server/journal-edit.ts`: `computeCorrections()` pure diff (normalisasi angka via Decimal → "1250.00" = "1250"), `validateEditLines()` (aturan sama jurnal manual + **id baris dipertahankan**), `editJournalLines()` (validasi → delete lines yang hilang → update/create sisanya → JournalCorrection createMany → ActivityLog `JOURNAL_EDITED` + diff). Status jurnal & task TIDAK berubah (revisi draft).
- API `POST /api/reviews/[taskId]/edit`: guard task PENDING + owner/admin, tenant-scoped.
- UI `components/queues/journal-lines-editor.tsx`: tabel editable (kode/akun/debit/kredit/keterangan), tombol tambah/hapus baris (min 2), live balance badge + total, error per baris; tombol "✏️ Edit" di tiap task queue (shortcut keyboard nonaktif saat mode edit).
- Insight A6 di quality page: card "Akun Paling Sering Dikoreksi" (top 10 akun, distribusi field, reviewer aktif) via `getCorrectionInsights()` di metrics.ts.
- Test +13 (204/204 hijau, 24 files), tsc/lint/build OK.
- Verifikasi live (browser): edit debit 3.900.000→3.950.000 & kredit 4.329.000→4.379.000 → JournalCorrection persis 2 baris (before/after benar) + ActivityLog correctionCount 2; edit nama akun Kas→"Kas & Bank" → 1 koreksi; save tanpa perubahan → 0 koreksi (diff bersih); stage tetap JUNIOR/SENIOR; quality page menampilkan insight (1-1000 Kas 1× · Nama akun · Admin Dev).
- **Pitfall baru (47)**: `validateEditLines` awalnya memetakan ke `ManualLineInput` (tanpa id) → SEMUA baris dianggap baru + lama dianggap terhapus → 20 koreksi noise per save + line id berubah tiap simpan. Fix: validasi wajib preserve `id` (EditLineInput).
- **Pitfall baru (48)**: bridge visible_browser TIDAK mendukung modifier key (Meta+A / Control+A gagal select-all, malah append). Cara aman edit text input: click → End → Backspace × panjang → type.
- Next: F2.5C 9 kategori dokumen (PRD M2).

## F2.5A lanjutan — akun jurnal terkunci ke COA klien (17:07–17:16, commit afddd2d)
- User tanya: apakah jurnal manual otomatis hanya bisa pilih COA milik klien terpilih? → Jawaban jujur: BELUM (free-text). Langsung ditutup.
- `coaAccountsFromMapping()` pure: ekstrak akun standar terpetakan dari `ClientProfile.coaMapping` (format `{kodeKlien:{accountCode,accountName}}`) → unik per accountCode + sort.
- `validateLinesAgainstCoa()`: mapping kosong → fallback free-text (klien belum dipetakan); mapping ada → akun asing DITOLAK (error jelas). Diterapkan di `createManualJournal` (sebelum create) + pre-check di UI.
- API baru `GET /api/clients/[id]/coa` (withTenantApi<Ctx> — params Promise, pola sama seperti profile route).
- UI journal-manager: pilih klien → fetch COA → baris jurnal jadi dropdown "— Pilih akun COA —" (label `kode — nama`); info "COA klien terpetakan: N akun"; fallback 2 input free-text + warning amber saat COA kosong; reset baris saat ganti klien (handleClientChange).
- Lint: set-state-in-effect → semua fetch dibungkus async start() + cancelled flag.
- Test +6 → 210/210, tsc 0, lint 0, build OK.
- Verifikasi live: autoglm (reuse tab 927227630) — dropdown 6 akun tampil + "COA klien terpetakan: 6 akun", pilih 1-1100 Kas Dr 500k / 4-1000 Pendapatan Cr 500k, "✓ Seimbang", submit (INTERACT → resume --auto-approve true) → jurnal "Jurnal manual COA test" MANUAL APPROVED di daftar; DB: 2 lines benar.
- Catatan autoglm: `--auto-approve` butuh nilai eksplisit (`--auto-approve true`); saat INTERACT_SENSITIVE_OPERATION resume wajib session_id+tab_id, tanpa start_url.

## Tenant hardening — skeleton multi-firma (17:19–17:30, commit 72a76af)
- User: "perkuat skeleton antar klien seperti antar tenant; 2-3 firma = 2-3 tenant, masing2 punya user & klien sendiri".
- Audit hasil: semua model data klien sudah firmId (Client, Document, JournalEntry, JournalCorrection, ActivityLog, SlaEvent, ClientProfile, WebhookSubscription, User) + JournalLine/ReviewTask/NotificationLog via relasi parent; SEMUA route internal pakai withTenantApi; Prisma extension auto-filter (TENANT_MODELS) aktif.
- GAP 1 (kritis): **OutboxEvent TIDAK punya firmId** → GET /api/outbox menampilkan event semua firma (bocor). Fix: tambah kolom firmId/clientId (nullable utk legacy) + relasi Firm + index; migrasi `outbox_tenant_firm_client`; backfill 5 event dari payload->>'firmId'; enqueueOutbox sekarang mengisi firmId/clientId dari payload.
- GAP 2: TENANT_MODELS kurang lengkap → ditambah ClientProfile, JournalCorrection, OutboxEvent, WebhookSubscription. KnowledgeItem TETAP global (KB = aset platform, semua firma berbagi standar akuntansi — keputusan didokumentasikan di kode).
- Script `scripts/seed-second-firm.ts` (idempotent, revert disertakan): firm 2 "PT Mitra Akuntan Nusantara" (slug mitra-akuntan), users admin.mitra@demo.dev + senior.mitra@demo.dev (password123), klien PT Sejahtera Bersama (READY 4 akun COA) + CV Karya Mandiri (LEARNING), 1 jurnal manual setoran modal 50jt (1-1100 Dr / 4-1000 Cr). Enum Industry: RETAIL|SERVICES|FNB (bukan bebas string!).
- Test +3 → 213/213; tsc 0; lint 0; build OK.
- Verifikasi live (side panel): login admin.mitra@demo.dev → dashboard KLIEN AKTIF 2 · TRANSACTIONS 1 (vs 22 firm 1) · SLA 0 · "Admin Mitra"; halaman Klien hanya CV Karya Mandiri + PT Sejahtera Bersama; Jurnal Manual hanya "setoran modal" (tidak ada jurnal firm 1). Lalu logout & login balik admin@ledgerline.dev (state demo normal, KLIEN 3/22 transaksi).
- Catatan: side panel bisa login multi-firma (logout → login), autoglm tab reuse tidak bisa ganti user tanpa logout manual. Pitfall lama (43) curl login tetap berlaku.
- Next: F2.5B edit saat review (PRD M1) — JournalCorrection model sudah ada di schema.

## F2.5C 9 kategori dokumen (18:31–18:36, commit e181603)
- Schema DocumentType + 8: FINANCIAL_STATEMENT, GENERAL_JOURNAL, PAYABLES_REPORT, RECEIVABLES_REPORT, INVENTORY_REPORT, PAYROLL_REPORT, TAX_REPORT, OTHER. INVOICE/RECEIPT jadi LEGACY (backward-compat — data lama tetap valid, tidak ditawarkan di UI).
- `src/ai/doc-type-map.ts` (pure): DOC_TYPE_LABELS 11 label Indonesia; ACTIVE_DOC_TYPES (9, urutan UI); LEGACY_DOC_TYPES; journalHintForDocType → {kind, template, hint} (PAYABLES→PURCHASE T-005, RECEIVABLES→SALES_CREDIT T-001, INVENTORY→PURCHASE, PAYROLL→T-007, TAX→T-008); DOC_KIND_PREFERENCE.
- rule-engine detectBusinessEvent: preferensi jenis dokumen = pilih rule terbaik DI ANTARA kind dalam preferensi (bukan sekadar filter best) + fallback deteksi umum. drafting.ts: hint disuntik ke prompt LLM.
- Semua konsumen docType pakai katalog bersama: documents route (11 jenis), portal route, upload-form (9 opsi, default BANK_STATEMENT), portal form, portal page & client detail label.
- Test +9 → 222/222; tsc 0; lint 0; build OK. Verifikasi live: dropdown upload = 9 kategori Indonesia; dokumen legacy Invoice tetap tampil.
- Pitfall: preferensi docType yang hanya filter best (bukan pilih ulang) gagal saat best di luar preferensi — mis. "pelunasan piutang" → SALES_CREDIT (keyword piutang) bukan RECEIPT.

## F2.5C 9 kategori dokumen (18:31–18:36, commit e181603)
- Schema DocumentType + 8: FINANCIAL_STATEMENT, GENERAL_JOURNAL, PAYABLES_REPORT, RECEIVABLES_REPORT, INVENTORY_REPORT, PAYROLL_REPORT, TAX_REPORT, OTHER. INVOICE/RECEIPT jadi LEGACY (backward-compat — data lama tetap valid, tidak ditawarkan di UI).
- `src/ai/doc-type-map.ts` (pure): DOC_TYPE_LABELS 11 label Indonesia; ACTIVE_DOC_TYPES (9, urutan UI); LEGACY_DOC_TYPES; journalHintForDocType → {kind, template, hint} (PAYABLES→PURCHASE T-005, RECEIVABLES→SALES_CREDIT T-001, INVENTORY→PURCHASE, PAYROLL→T-007, TAX→T-008); DOC_KIND_PREFERENCE.
- rule-engine detectBusinessEvent: preferensi jenis dokumen = pilih rule terbaik DI ANTARA kind dalam preferensi (bukan sekadar filter best) + fallback deteksi umum. drafting.ts: hint disuntik ke prompt LLM.
- Semua konsumen docType pakai katalog bersama: documents route (11 jenis), portal route, upload-form (9 opsi, default BANK_STATEMENT), portal form, portal page & client detail label.
- Test +9 → 222/222; tsc 0; lint 0; build OK. Verifikasi live: dropdown upload = 9 kategori Indonesia; dokumen legacy Invoice tetap tampil.
- Pitfall: preferensi docType yang hanya filter best (bukan pilih ulang) gagal saat best di luar preferensi — mis. "pelunasan piutang" → SALES_CREDIT (keyword piutang) bukan RECEIPT.

## F2.5D Trial Balance interaktif (18:39–18:46, commit c1c3bf9)
- `src/server/trial-balance.ts` pure: parsePeriod("YYYY-MM"), prevPeriodOf (lintas tahun), classifyAccount (digit pertama: 1 aset, 2 liabilitas, 3 ekuitas, 4 pendapatan, 5 beban, lain→LAINNYA), NORMAL_BALANCE (aset/beban DEBIT; liabilitas/ekuitas/pendapatan KREDIT), buildTrialBalance (agregasi per akun, saldo sesuai normal, indikator kewajaran: aset kredit → "Saldo kredit pada akun aset/beban", piutang negatif (nama mengandung piutang + net negatif) → alasan khusus, liabilitas/ekuitas/pendapatan debit; komparatif bulan lalu; total debit=kredit + balanced flag EPS 0.005), trialBalanceCsv (BOM UTF-8) + trialBalanceXlsx (xlsx lib).
- API GET /api/clients/[id]/trial-balance?period=YYYY-MM&format=json|csv|xlsx — withTenantApi, OPERATIONAL_ROLES, klien wajib firmId cocok; default periode bulan berjalan.
- UI /dashboard/reports/trial-balance + trial-balance-view.tsx: filter klien (auto-select pertama) + input month, kartu Total Debit/Total Kredit/Status Seimbang/Indikator Kewajaran, tabel (kode/akun/klasifikasi badge/debit/kredit/saldo/bulan lalu/⚠ indikator), footer total, ekspor CSV/XLSX link; empty state "Belum ada data"; sidebar Analitik + "Neraca Percobaan".
- Lint pitfall baru: react-hooks/set-state-in-effect flag `useEffect(() => { void load(); })` & `void loadClients()` meski setState di useCallback; pola aman = `async function start() { await load(); } void start();` (sama seperti journal-manager).
- Test +16 → 238/238; tsc 0; lint 0; build OK. Verifikasi live: dropdown native via autoglm select_dropdown_option; PT Maju Jaya 2026-08 → Total Debit = Total Kredit = Rp 10.629.000, Seimbang; CV Berkah Abadi 0 akun (tidak ada jurnal APPROVED) — benar.

## F2.5E Buku Besar + Reclass + Kunci Periode (18:58–19:35, commit cdd083c)
- Schema: JournalStatus+FINALIZED; model FiscalPeriod (clientId+period @@unique, status OPEN/CLOSED, lockedById/lockedAt, relasi Firm/Client/User "PeriodLocker"); migrasi fiscal_period_lock.
- `src/server/ledger.ts`: getLedger (buku besar per akun+periode; running balance; total; status periode), getFiscalPeriodStatus, lockPeriod ($transaction: upsert CLOSED + journalEntry.updateMany APPROVED→FINALIZED), reclassJournal (guard: hanya APPROVED, periode tidak CLOSED → "perbaiki lewat jurnal penyesuaian", journalIsBalanced, validateLinesAgainstCoa; deleteMany+createMany lines + ActivityLog JOURNAL_EDITED before/after), periodOf. journal-machine TRANSITIONS + APPROVED→FINALIZED + FINALIZED:[] (terminal); seed REVIEW_FLOW+FINALIZED:null.
- trial-balance fetchEntries: status IN (APPROVED, FINALIZED); report.periodStatus (dari getFiscalPeriodStatus).
- API: GET /api/clients/[id]/ledger?accountCode&period; GET /api/clients/[id]/periods/[period] (partner/admin); POST .../lock (partner/admin) — PENTING: file di [period]/route.ts tidak menangani /lock → harus folder [period]/lock/route.ts (404 kalau tidak); GET /api/journals/[id] (detail+lines); PATCH /api/journals/[id] (reclass; EDIT_JOURNAL_ROLES = ADMIN/SENIOR/PARTNER).
- roles.ts baru: PARTNER_ROLES (ADMIN/PARTNER), EDIT_JOURNAL_ROLES.
- UI: TB — kode akun jadi <Link> ke ledger (encodeURIComponent), badge periode terbuka/terkunci, tombol "🔒 Kunci Periode" (window.confirm; canLock dari page server via PARTNER_ROLES); /dashboard/reports/ledger + ledger-view.tsx (useSearchParams clientId/accountCode/period, tabel saldo berjalan, tombol Reclass per baris → modal editor baris (fetch detail jurnal, live balance, tambah/hapus baris), banner periode terkunci + link jurnal penyesuaian, kembali ke TB); sidebar + Buku Besar.
- Fix: THead komponen SUDAH render <tr> — jangan menaruh <tr> lagi di dalamnya (React warning nested <tr>).
- Test +10 → 248/248; tsc 0; lint 0; build OK.
- Verifikasi live (autoglm tab 927227637 + side panel): drill-down TB→Buku Besar Kas (1-1000) Maju Jaya; lock via autoglm klik Kunci Periode + dialog OK → psql: FiscalPeriod 2026-08 CLOSED + 3 jurnal FINALIZED; UI: badge 🔒 terkunci, banner reclass-off, tombol Reclass hilang, TB tetap hitung FINALIZED (total Rp 10.629.000 tetap tampil).
- Catatan: window.confirm bisa dihandle autoglm (klik OK); visible_browser bridge tidak bisa native select/confirm — pakai autoglm untuk itu.

## KB & skill — format neraca lajur & laporan keuangan (19:32–19:50, commit 10acd17)
- User upload 3 file patokan: Worksheet-akuntansi (neraca lajur 10 kolom, contoh ajp), Template-Laporan-Keuangan-Sederhana-untuk-UMKM.xlsx (12 sheet: jurnal umum, buku besar, neraca saldo, laba rugi single/multi step, perubahan modal, neraca T/staffel, arus kas langsung/tidak langsung, COA saldo normal), Unilever annual-reports-2025.pdf (laporan keuangan audited PT Unilever Indonesia Tbk, bilingual, jutaan Rp).
- Skill keuangan-akuntansi-indonesia: + references/07-format-laporan-keuangan.md (ditulis via exec — write tool terblokir ke luar workspace; pola: tulis ke .openclaw/tmp dulu lalu cp) + peta materi SKILL.md baris 07.
- KB Praktis: scripts/seed-kb-laporan.ts idempotent → 3 KnowledgeItem "Referensi Laporan" ACTIVE (format-neraca-lajur, format-laporan-umkm, format-laporan-publik w/ angka Unilever 2025: aset 20.017.339 = liab 15.542.221 + ekuitas 4.475.118; laba 7.641.161; EBITDA 5.260.715; EPS 93). Live: halaman KB 16 aturan aktif.
- Angka kunci Unilever 2025 (juta Rp) utk patokan format publik: penjualan bersih 31.943.461; HPP 16.946.367; laba bruto 14.997.094; laba usaha 4.592.021; pajak 960.983.

## Adaptasi keseluruhan annual report Unilever 2025 — narasi & dashboard (19:39–19:55, commit f5ab41f)
- User minta adaptasi KESELURUHAN report Unilever (bukan cuma laporan keuangan): narasi + dashboard.
- Baca PDF 482 hal: daftar isi (struktur SEOJK 16/2021 11 bagian), Kinerja 2025 (6 KPI), Ikhtisar 5 tahun (laba rugi + posisi + 13 rasio), Ikhtisar saham per kuartal + buyback, Laporan Direksi (makro: GDP 5,11% / inflasi 2,92% / BI-Rate 4,75%; strategi Category-Channel-Cost; Our Formula for Desire: Desire at Scale/Play to Win/Next Wave), CAPEX 1,32T, divestasi es krim (8 Des 2025) & teh SariWangi (2 Mar 2026), prospek.
- Skill: references/08-adaptasi-annual-report-unilever.md (struktur POJK, dashboard KPI, ikhtisar 5 tahun, ikhtisar saham, kerangka narasi direksi, template narasi klien 9 langkah, checklist) + SKILL.md baris 08.
- KB: seed-kb-laporan.ts + item ke-4 format-annual-report-narasi-dashboard (ACTIVE) → 4 aturan "Referensi Laporan". Verifikasi psql OK; tsc/lint 0.
- Angka kunci dashboard Unilever 2025: Penjualan 31,9T (+4,3%), Laba bersih 3,5T (cont.)/7,6T (total), GPM 46,9%, OPM 14,4%, ROA 19,6%, ROE 106,8%, EBITDA 5,26T (16,5%), EPS 93; kapitalisasi pasar Q4 Rp99,19T; buyback Rp2T @maks 1.700.

## F3 · Portal Klien (K1–K6) SELESAI — commit f3-portal
- Migrasi `f3_portal_snapshot_notif`: model `ReportSnapshot` (clientId+period+type+version @@unique, payload Json) & `ClientNotification` (type REPORT_READY/DOCUMENT_PROCESSED/EXCEPTION/REMINDER, readAt).
- `src/server/simple-explain.ts` (pure): classify digit-1 COA → frase sederhana; explainJournal (kalimat per baris) + summarizeJournal (ringkasan Rp).
- portal.ts: findDuplicateDocument (K2), getPortalJournals (K3), getPortalTimeline (K1), listReportSnapshots/createReportSnapshot (K5), listClientNotifications/markNotificationsRead/notifyClient (K4).
- Lock route: setelah lock sukses → snapshot TB + notifikasi REPORT_READY (gagal snapshot tidak menggagalkan lock).
- API portal baru: /journals, /timeline, /snapshots (?id&format=csv|xlsx), /notifications (GET/POST); upload documents route → dedupe 409.
- UI portal: Notifikasi (badge unread + tandai dibaca), Status Dokumen (timeline steps), Transaksi Saya (summary + details), Laporan & Versi (unduh CSV/XLSX via <a> — hindari window.location.href karena lint no-location-assign), Privasi (K6).
- Relasi ReviewTask dari JournalEntry = `reviewTask` (via journalEntryId), bukan `reviewTasks` (via assigneeId) — tsc catch.
- Lint: window.location.href → error react-hooks/immutability + no-location-assign; pakai <a href>.
- Test 253/253 (+5 simple-explain); tsc 0; lint 0; build OK. Seed `scripts/seed-f3-portal.ts` (idempotent).
- Live verifikasi: 3 jurnal read-only Maju Jaya, timeline 1 dokumen PROCESSED, snapshot TB v1, 2 notif unread, dedupe 409 ✓. Token demo Maju Jaya: 01882943-061c-412c-b5c7-76d098dd3a9b.

## EN-06 selesai — Exception one-screen (commit en06-one-screen)
- `GET /api/exceptions/[id]`: detail exception (journal + client + document + lines + rule re-detect via detectBusinessEvent; rule pakai EVENT_RULE_LABELS baru di rule-engine: label Indonesia, template, psakRef).
- `GET /api/documents/[id]/file`: download dokumen sumber terenkripsi (withTenantApi + OPERATIONAL_ROLES; 401 anon; 200 PDF valid dengan sesi; cleanup test doc via SQL).
- exceptions-list.tsx: panel resolve → grid 3 kolom (Dokumen Sumber | Aturan & Alasan | Draft Jurnal AI) + form note + tombol selesaikan. Layout lg:grid-cols-3.
- Pelajaran: dokumen seed demo (invoice-majujaya-0812.pdf) cuma path tanpa file fisik → 500 "mungkin rusak" saat download (bukan bug route). Upload API pakai field `docType` (bukan `type`). INVOICE legacy ditolak upload API baru (pakai BANK_STATEMENT dll).
- Test 254/254 (28 files); tsc 0; lint 0; build OK. Verifikasi live: panel one-screen CV Berkah Abadi — dokumen sumber (dengan lampiran sementara), aturan T-005 · PSAK 14 · skor 33%, draft 2 baris Rp16,5jt, tombol buka dokumen.
